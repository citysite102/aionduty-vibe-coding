{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '(.tool_input.content // \"\") + \"\\n\" + (.tool_input.new_string // \"\")' | grep -q '——' && printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"偵測到中文破折號「——」，請改用「，」「。」「：」或（）括號取代，不要使用破折號。\"}}' || true",
            "statusMessage": "檢查是否含破折號「——」"
          }
        ]
      }
    ]
  }
}
