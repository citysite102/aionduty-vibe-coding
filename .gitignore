node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# 課程籌備用的內部文件，留在本機、不對外公開
審稿報告*.md
報名問卷_*.md
邀請文_*.md
課程銷售頁_*.md
案例網站_*.md
Banner_Prompts_*.md
設計規範稽核報告.md
教學模擬報告_*.md
教學模擬報告_*.pdf

# 教學模擬的產出。每次跑都會重生，而且裡面是模擬學員的逐頁反應，
# 屬於上面那一類的內部文件。sim/README.md 與 sim/personas/ 是設定，要進版控。
sim/transcript/
sim/reactions/
sim/state/
sim/findings/
sim/handoff.md
# 舊輪次的歸檔（頁碼已與現況對不上，只留在本機供對照）
sim/archive/

# AI Studio / Codex 的本機專案設定
.openai/
.claude/settings.local.json

# 設計稽核工具的本機狀態。config.json 是共用設定，要進版控；
# 這兩個是這台電腦自己的（同意紀錄與掃描快取）。
.impeccable/config.local.json
.impeccable/hook.cache.json

# 逐字稿 PDF（npm run pdf 產出）2026-09-22 起進版控，讓沒有開發環境的人
# 直接在 repo 裡拿得到最新的一份。它是衍生檔，唯一的真相來源仍是 逐字稿/*.md。
#
# 代價要知道：它是 6.9 MB 的二進位檔，PDF 幾乎沒有 delta 壓縮，
# 每 commit 一次就多存一整份。所以**不要每改一句就重新產一次進版控**，
# 累積到一個段落再跑 npm run pdf 並跟著那一批改動一起 commit。
# 只有 PDF 變動、markdown 沒動的 commit 沒有意義。
