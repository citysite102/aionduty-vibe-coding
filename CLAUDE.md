# 專案共用規範

本專案有兩個性質不同的區塊，套用的規範不同：

| 區塊 | 路徑 | 技術 | 規範在哪 |
|---|---|---|---|
| 簡報投影片（主要產出） | `src/slides/`、`src/components/` | React + Tailwind + `motion/react` | 這份，A、B、D 章 |
| Remotion 動畫元件 | `src/remotion/` | Remotion，frame 驅動 | `src/remotion/CLAUDE.md`，加這份的 A-3、D 章 |
| 現場工具（倒數計時器） | `src/components/CountdownOverlay.tsx`、`LetterField.tsx` | React + canvas | 只套 D 章。**A-3 的動態規範不適用**，見下方 |

> Remotion 那份是子目錄手冊，Claude Code 動到那一區的檔案時才會載入，平常不佔 context。**不要把它的規則套到投影片上**，投影片沒有 frame 的概念，節奏是靠 `currentStep`。

> 倒數計時器是疊在投影片上的一層，講者按 `T` 或點操作列上的碼錶叫出來。它會透過 `onActiveChange` 通知 `App.tsx` 把翻頁的鍵盤與點擊讓出來，**改 `App.tsx` 的 `handleKeyDown` 或 `handleContainerClick` 時不要把那兩個 `if (timerActive) return` 拿掉**，否則講者在計時畫面按空白鍵，底下的投影片會偷偷跳頁。它的字母場是持續動的，這是它的功能本身，不受 A-3「禁止常駐無限動畫」規範；那條規範是為了投影片內容不要跟講者搶注意力。背景音樂放 `public/music/`，換檔案要同步改 `CountdownOverlay.tsx` 的 `TRACKS`。

---

## A. 投影片規範（`src/slides/`）

### A-1 色彩

底色與文字一律走 Tailwind `slate` 階：

- 背景：`bg-[#020617]`（App 層已設定，投影片不要自行覆蓋整頁背景）
- 卡片／區塊：`bg-slate-900`，邊框 `border-slate-800`
- 主文字：`text-slate-100`／`text-slate-200`
- 次要文字：`text-slate-300` 到 `text-slate-500`

強調色只有一個主色，其餘色相僅限「語意用途」：

| 用途 | 色相 | 說明 |
|---|---|---|
| 主 accent | `sky` | 標題 icon、副標、當下要強調的那一個元素 |
| 對照的另一邊 | `indigo` | 僅在同一頁有成對對照時使用，見下方 |
| Claude 專有名詞 | `orange` | 標身分不標重點，見下方 |
| 成功／正解 | `emerald` | 僅在有正反對照時使用 |
| 警告／注意 | `amber` | 僅在有風險提示時使用 |
| 錯誤／禁止 | `red`／`rose` | 僅在示範錯誤做法時使用 |

**同一頁最多兩種強調色**（`orange` 不計入，理由見下方）。沒有語意的地方就用灰階，顏色越少越乾淨。

上表以外的色相一律不要新增。特別注意這三組，它們長得跟合法色很像，最容易混進來：

- `blue` 不是 `sky`、`green` 不是 `emerald`。差一階但色溫不同，混在同一頁會看起來像沒對齊。
- `gray`、`zinc`、`neutral`、`stone` 都不是 `slate`。全片灰階只有 `slate` 一種。
- `violet`、`purple`、`teal`、`cyan`、`pink`、`fuchsia`、`lime`、`yellow` 直接不要用。

驗收用這行掃，只該印出上表那七個色相（`slate`、`sky`、`indigo`、`orange`、`emerald`、`amber`、`red`／`rose`）：

```bash
grep -rhoE --include="*.tsx" -- \
  "-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-[0-9]" \
  src/slides src/slides-recorded | sed -E 's/^-//; s/-[0-9]$//' | sort | uniq -c | sort -rn
```

色相要一個一個列出來（這是 Tailwind 全部 22 個），不要偷懶寫成 `[a-z]+`。寫成萬用會把 `duration-500`、`z-50`、`border-l-…` 的 `l` 都算進來，跑出一堆雜訊，久了就沒人看了。

2026-08-04 起這行是乾淨的，只會印出那七個。**多印任何一個就是違規，不會有例外**，不要為了留一個孤兒檔而把它當成已知誤報放著。

#### `indigo` 的職務：成對對照的第二邊

`indigo` 只有一個用途：一頁裡有兩個並列、需要互相對照的東西時，第一邊用 `sky`，第二邊用 `indigo`。

已經在用這個模式的頁面：

| 頁面 | 第一邊（`sky`） | 第二邊（`indigo`） |
|---|---|---|
| `10c_M1_WebArch` | 前端 (Client) | 後端 (Server) |
| `10c2_M1_WebArchDuties` | 前端負責 | 後端與資料庫 |
| `06_Threads` | Thread A | Thread B |
| `31_Cheat_Dials` | 旋鈕二：邊界大小 | 旋鈕一：監督程度 |

已知還沒改的一頁：`13_M1_Example2`（執行前／執行後）的第一邊是灰階不是 `sky`，所以那個 `indigo` 其實是下面說的「單一強調」，該改成 `sky`。列在這裡是為了不要再被當成正確範例抄。（`09_M1_CodeIntro` 原本也在這個名單上，2026-08-10 已改成 `sky`。）

判斷方式很簡單：**如果拿掉 `indigo` 那一邊，畫面上就沒有東西在跟它對照，那就不該用 `indigo`。**

因此以下三種都不算對照，要改掉：

1. **單一強調**：整頁只有一個重點，卻標成 `indigo`。那是 `sky` 的工作。
2. **項目編號**：三、四個平等的項目各給一色，`indigo` 只是其中一格。平等的項目一律灰階，只讓當下要講的那一個亮起來。
3. **純裝飾**：漸層、光暈、背景色塊裡的 `indigo`，沒有掛在任何文字或概念上。直接刪掉。

`sky` 與 `indigo` 成對出現時**合計算一種強調色**，因為它們是同一個對照關係的兩邊。所以「前端 sky / 後端 indigo / API emerald」的三段式圖解是合法的。單獨出現的 `indigo`（沒有 `sky` 跟它配對）則算違規，不是算一種。

#### `orange` 的職務：Claude 專有名詞

`orange` 跟其他強調色不同，**它標的是「這東西是誰家的」，不是「這東西現在重要」**。所以它不計入一頁兩色的額度，就像灰階不計入一樣。

只有三種用法：

1. **行內的產品專有名詞**，一律等寬字：斜線指令（`/help`、`/context`）、Claude Code 的按鍵（`@`、`Shift + Tab`）、`CLAUDE.md`、`SKILL.md`、Skill 名稱。
2. **成對對照裡「Claude 那一邊」**，此時另一邊用 `emerald` 或灰階。例如 `32_Cheat_Tools` 的「Claude 生態系 vs 開源通用陣營」。
3. **仿真 Claude Code 真實畫面時照抄它的橘**（`11b_M1_ClaudeCodeUI`）。學員要拿它對照自己的螢幕，改色反而是錯的。

不要這樣用：

- **不要拿它當重點色。**「這一句要記住」是 `sky` 的工作。同一頁如果橘字比 sky 還多，代表你在用它強調，不是在用它標身分。
- **不要整塊卡片染橘**，除非那一塊真的在跟非 Claude 的東西對照（上面第 2 種）。邊框、背景、標題染橘會讓它變成強調色。
- **不要標通用名詞。**`git commit`、`npm run lint`、`.env` 不是 Claude 的東西，維持灰階。`Shift + Tab` 算，因為那是 Claude Code 定義的操作。

判斷方式：**把那個 token 換成別家工具的對應物，句子還成立嗎？**成立就不是專有名詞，不該上橘。

#### 產品 Logo 走 `BrandLogos`，品牌色不佔額度

列工具的頁面（`01b_PreFlight` 那種）不要拿 lucide 的通用 icon 代替產品。學員要拿畫面上的圖示去對照自己桌面上的那一個，`SquareTerminal` 對不上 Warp，`MonitorDown` 也對不上 Claude。用 `src/components/BrandLogos.tsx`：

- `<BrandTile brand="claude" />` 是連底色方塊一起的版本，直接替掉原本包 icon 的那個 `span`。
- `<BrandLogo brand="git" size={18} />` 只出字形，顏色吃 `currentColor`，要塞進句子或自訂容器時用它。
- 要加新的牌子就在 `BRAND_PATH`、`BRAND_COLOR`、`BRAND_LABEL` 各補一條。路徑取自 simple-icons（CC0），**要內嵌進那個檔案，不要 import 套件、也不要連外部網址**（A-4）。

品牌色不走 Tailwind 色階，寫成 hex 的 inline style。**它不計入一頁兩色的額度**，理由跟 `orange` 那條同一個：它標的是「這是誰家的東西」，不是「這東西現在重要」。也因為是 inline style，上面那行色相 grep 掃不到它，不會影響驗收。

兩個要注意的：

- **官方色在深底上看不見就要換。** GitHub 的 `#181717` 與 Warp 的黑底白字直接消失，那兩個取它們深色模式下的白。判準是看得見，不是照抄色票。
- **這個豁免只給真正的產品 Logo。** 自己畫一個圖形然後配一個 hex，那是繞過 A-1，不是標身分。

#### 訊息塊一律用 `Callout`，不要自己拼

要跟周圍內文區隔開的那種塊（結論、但書、正解、錯誤示範）走 `src/components/Callout.tsx`，用 `tone` 選語意：`focus`（重點）、`good`（正解）、`warn`（風險）、`bad`（錯誤做法）、`muted`（次要補充，維持灰階）。內層版面特殊、不方便換成元件時，至少用它匯出的 `calloutClass()` 取得同一組 class。

樣式是**整框淡色底加同色邊框**，不是左側色條。色條只在框的一邊，視線掃過去容易漏掉，而且它跟卡片自己的 `border` 疊在一起會變成「有一條線比較粗」而不是「這一塊比較重要」。2026-08-04 全片 40 個框改成整框染色，`border-l-4` 已經歸零，**不要再新增**。

`focus` 的 sky 底色不計入一頁兩色的額度，因為它取代的就是原本那條 sky 色條，沒有多出色相。

字級：live 頁內文 `text-base`、附註 `text-sm`；預錄拆頁傳 `size="rec"`，內文升到 `text-xl`。不要為了「看起來比較有份量」在 live 頁用 `rec`。

### A-2 節奏與分段

- 投影片內的分段一律用 `AnimatedBlock` 的 `stepIndex`，由 `currentStep` 驅動。
- **嚴禁用 `setTimeout` / `setInterval` 驅動畫面內容切換**。講者講到一半畫面自己跳走是嚴重問題。需要「切換分頁」這類效果時，用 `currentStep >= N` 決定預設值，再讓講者可手動覆蓋。
- `stepIndex` 由 1 開始，同一頁內遞增。左右兩欄要同時出現才可共用同一個號碼，否則不要重號。
- **刪掉一整個 `AnimatedBlock` 之後，同頁剩下的號碼要重編成從 1 開始的連續號**，包含 `.map()` 裡 `stepIndex={i + N}` 的那個 `N`。留下缺號不會報錯也不會被 typecheck 抓到，但講者現場會多按一次卻沒有任何反應，而且缺的通常是第 1 號，也就是進到那一頁按第一下沒東西出現。

### A-3 動態原則（防 AI 味的負面清單）

1. **一次只動一個重點，其他元素保持靜止。**
2. **禁止常駐無限動畫。** 具體包含：
   - Tailwind：`animate-pulse`、`animate-ping`、`animate-bounce`、`animate-spin`
   - motion：`transition={{ repeat: Infinity }}`
   - 特別注意 `.map()` 裡的條件式 class，一行程式可能生出七個閃爍點。
   - 例外：真的要表達「系統正在運轉」時，全頁最多留一組，且必須是慢速、低對比。
3. 進場用 opacity 0→1 加小位移（10 到 16px）。不要大幅飛入、不要旋轉進場、不要 `scale: 0→1` 彈跳。
4. easing 全片統一，沿用 `AnimatedBlock` 的 `easeOut`。要用 spring 就把 damping 調高，不要 Q 彈。
5. 每個重點停留夠久讓人讀完，關鍵文字至少停 1.5 到 2 秒。
6. 不要把「fade-in 加 slide-up」套在每個元素上，寧可少動。

### A-4 Tailwind 與資源

- **只用內建色階。** `slate-850`、`sky-350` 這類不存在的色階不會報錯、typecheck 也會過，但邊框會直接不渲染。
- **只用合法的間距 class。** `py-0.2`、`px-1.7` 等同樣是靜默失效，寫 `py-0.5`。
- **只用 Tailwind 內建的 animate class。** `animate-spin-slow`、`animate-infinite`、`animate-duration-2000` 都是別的外掛才有的，這個專案沒裝，寫了不會報錯也不會動。要調速度用 `style={{ animationDuration }}`，或直接改用 `motion`。（這三類是同一種病：class 拼錯不會壞，只會安靜地什麼都不做。）
- **只用內建字級，不要寫 `text-[13px]` 這種。** 內建階之間本來就有節奏，自訂 px 會讓同一頁出現三種只差一兩 px 的大小，看起來像沒對齊。**投影片內文最低到 `text-sm`**，`text-xs` 留給註解、膠囊標籤與圖表註記，不要拿來寫成段的句子。2026-08-04 全片把 `text-[8px]` 到 `text-[10px]` 那九處清乾淨了，不要再長回來。
- **JSX 裡的反引號是純文字，不是程式碼標記。** 寫 `` <p>`/init` 產出的是⋯</p> `` 畫面上會出現兩個反引號，Markdown 那一套在這裡不會生效。要等寬就寫 `<code className="font-mono text-orange-300">/init</code>`。這個錯犯過兩次，掃描用 `grep -rnoE '>[^<]*\`[^\`]+\`[^<]*<' --include='*.tsx' src`。
- **`mt-auto` 在等高卡片裡可能等於 0。** 一排卡片用 grid 撐成等高時，如果每張的內容一樣長，就沒有多餘高度可以分配，`mt-auto` 推不動任何東西。想讓頁尾的分隔線跟上面那段保持距離，間距要自己給（上面 `mb-4`、下面 `pt-4`），不能只靠 `mt-auto`。
- **內容區的外層要用 `min-h-full`，不要用 `h-full`。** `SlideLayout` 的內容區是 `overflow-y-auto`，外層寫 `h-full` 加 `justify-center` 時，內容一旦超過高度就會上下同時溢出，**上緣會被推到捲動範圍外面，捲不到也看不到**。`min-h-full` 是「至少這麼高」，超過就往下長，捲動正常。這種錯不會報錯也不會被 typecheck 抓到。
- **不引用外部網址資源**（圖片、字型、背景貼圖）。現場離線播放會破圖，一律放 `assets/`。
- 不要留不可點的裝飾性按鈕，現場真的會有人去點。用 `div` 或加上真實行為。
- 提交前四支都要跑：`npm run lint`、`npm run check:slides`、`npm run check:rec`、`npm run check:words`。注意 `lint` 只是 `tsc --noEmit`，抓不到上面那三類 class 錯誤，那要靠肉眼或上面那行 `grep`。

---

## B. 新增或搬動投影片的檢查清單

### B-0 先看懂兩層結構

畫面上的頁面順序不是手寫的，是推導出來的：

```
LIVE_SLIDES  ─┐
LIVE_TITLES  ─┼─→ ENTRIES ─→ SLIDES / SLIDE_TITLES（畫面實際播的）
REPLACEMENTS ─┘                  ↑
                          UNIT_DEFS 標在上面，切出一支一支影片
```

- **`LIVE_SLIDES` / `LIVE_TITLES`**（`src/App.tsx`）是「未拆頁」的順序，兩個陣列靠 index 對齊，長度必須一致。
- **`REPLACEMENTS`**（`src/slides-recorded/registry.ts`）的 key 是 **LIVE index**，把那一頁換成一組預錄頁。這個數字會變，要看現況就跑 `npm run check:slides`，第一行會印「LIVE N 頁，其中 X 頁拆成 Y 頁」。
- **`SECTION_DEFS`** 的 `start` 也是 **LIVE index**，指向一張分節頁（`*_Div_*.tsx`）。下拉選單的 `optgroup` 由它自動推導，**不需要手動維護 slice 範圍**。
- **`UNIT_DEFS`**（`src/courseUnits.ts`）是錄製單元，一筆就是一支要單獨錄的影片。它寫的是「從哪一頁開始」，用 `live`（LIVE index）加 `part`（那一頁被拆成好幾頁時的第幾頁，0 起算），結束點是下一筆的前一頁，所以不會有缺口或重疊。

`SLIDES`、`SLIDE_TITLES`、`SECTIONS` 都是衍生值，不要直接改。**單元編號 `X-Y` 也是衍生的**：X 是它落在第幾章（八個分節＋結語＝九章），Y 是它在那一章裡的第幾個，搬頁之後會自己重排，任何地方都不要把 `3-4` 這種編號寫死。

### B-0b 章節、單元、階段是三個東西

同一批頁面被三種粒度切過，寫文案的時候不要混用：

| 詞 | 指的是 | 定義在哪 | 有幾個 |
|---|---|---|---|
| 章節 | 分節，也就是講者選單的分組 | `SECTION_DEFS` ＋結語 | 9 |
| 單元 | 一支單獨錄的影片，編號 `X-Y` | `UNIT_DEFS` | 用 `npm run units` 看 |
| 階段 | `02_Philosophy` 那四張卡的課程主軸 | 那一頁自己 | 4 |

**投影片內文只講章節，不要寫「第 N 單元」。**「單元」是錄製與課程平台在用的，而它的編號會隨搬頁重排，寫進內文就會指錯。要指前面做過的東西，最穩的是直接講那個東西（「你做計時器的時候」），其次才是章節。

這件事踩過一次：內文的「第一單元」指的是 `02_Philosophy` 的第 02 張卡，兩邊編號差一號，沒有人發現。

### B-1 插一頁 live 頁面

1. 在 `src/slides/` 建檔，檔名沿用 `編號_模組_主題.tsx` 慣例。
2. 在 `App.tsx` 上方加 `import`。
3. 插進 `LIVE_SLIDES` 的正確位置。
4. 在 `LIVE_TITLES` 的**完全相同位置**插標題。
5. **把所有「大於等於插入位置」的 index 往後推 1**，這是最容易漏的一步：
   - `SECTION_DEFS` 的每個 `start`
   - `REPLACEMENTS` 的每個 key（連同它上面的 `// index N = 原「⋯」` 註解）
   - `UNIT_DEFS` 的每個 `live`
6. 想清楚新這一頁屬於哪個單元。接在既有單元中間就什麼都不用做；如果它自己要另起一支影片，在 `UNIT_DEFS` 加一筆，`anchor` 抄它畫面上實際的標題。

刪頁就是同樣的事往前推 1。**在陣列尾端加頁、或是往 `REPLACEMENTS` 既有的 key 底下多塞一頁預錄頁，都不會造成位移**，可以跳過第 5 步。

搬頁（不是插頁）也要看第 6 步：一頁換到別的單元裡，那兩支影片的內容都變了，順手確認它的承接語還指得到（B-4）。

### B-2 跑自檢，不要靠肉眼數

```bash
npm run check:slides
```

它會抓五類「不會報錯、typecheck 也抓不到」的錯：

| 檢查 | 抓什麼 |
|---|---|
| 兩個陣列長度 | `LIVE_SLIDES` 與 `LIVE_TITLES` 不一致 |
| `REPLACEMENTS` key | key 指到了別頁（拿註解裡的標題比對 `LIVE_TITLES[key]`） |
| `SECTION_DEFS` start | start 沒有落在分節頁上 |
| 分節頁涵蓋率 | 有 `*_Div_*.tsx` 卻沒有對應的 `SECTION_DEFS`，講者在選單裡會找不到那一節 |
| `UNIT_DEFS` | 單元起點指到別頁（拿 `anchor` 比對實際標題）、順序亂掉、或有單元跨章 |

**每個 `REPLACEMENTS` 項目上面都要有 `// index N = 原「標題」` 註解，每一筆 `UNIT_DEFS` 都要有 `anchor`。** 那是自檢唯一能核對它有沒有指錯的依據，少了就看不出來，所以缺了會直接報錯。單元指錯尤其安靜：它不會壞掉，只會讓那一支影片從別的地方開始錄。

改完順序也跑一次 `npm run lint` 與 `npm run check:rec`。三支都過再提交。

### B-2b 檔名裡的編號不是頁碼

`10e`、`11b`、`21f4` 這些是**主題群組編號**，不是播放順序。同一群的檔案排在一起是它唯一的用處。

**不要為了讓檔名對上頁碼而改檔名。** 位置正是最常變的東西：插一頁就要重新命名它後面的每一個檔案，連帶 `App.tsx` 的 import、`REPLACEMENTS`、還有註解裡的交叉引用（例如「那三個搬去 `11d2_M1_TerminalKeys`」）全部要跟著改，而且 git 的檔案歷史會被切斷。順序的唯一真相是 `LIVE_SLIDES`，那已經有 `check:slides` 在把關。

要從畫面上的頁碼找到檔案，用：

```bash
npm run map           # 全部，依分節分組
npm run map -- 29     # 只看第 29 頁
npm run map -- git    # 標題或檔名含 git 的頁
```

它印的 Slide 編號跟畫面右下角一致（已把預錄拆頁算進去），LIVE 那一欄才是插頁時要改的 index。

### B-2c 錄製清單用 `npm run units` 印，不要另外手抄一份

```bash
npm run units           # 全部，依章節分組
npm run units -- 3      # 只看第三章，連每一頁的標題一起印
npm run units -- 3-4    # 只看那一個單元
npm run units -- 手冊    # 章節名或單元名含「手冊」的
```

它印的是「章節 ／ 單元編號 ／ Slide 幾到幾 ／ 幾頁 ／ 單元名稱」。**要給剪接或上架用的清單就從這裡複製，不要在別的文件裡再抄一份頁碼**，理由跟 B-3 同一條：抄出去的那份不會跟著搬頁更新，而播放器右下角一直顯示著正確的頁碼。

播放器操作列上的下拉選單也是照單元分組的，右邊那個小小的 `單元 3-4` 就是目前這一頁屬於哪一支影片。錄的時候看它就知道什麼時候該停。

**不要為每個單元加一頁封面。** 單元的第一頁本來就是一張帶大標題的 `SlideLayout`，那已經是標題卡了，再插一頁只寫單元名等於同一句話講兩次（D-2 的判準）。而且封面頁會把推導出來的邊界變回寫死的檔案，插頁時又要搬檔案、改 import、推 `REPLACEMENTS` 的 key。

需要的那個記號已經有了：每個單元的第一頁右上角會出現灰色的「單元 3-4」，由 `SlideContext` 的 `unitMark` 帶進去，`SlideLayout` 與 `SectionDivider` 各接一次，錄製模式（`?clean=1`）照樣顯示，因為它就是要出現在影片畫面上的東西。封面那一頁不掛，整堂課的封面上再標一次是噪音。

反過來說，**單元的第一頁要自己說得出這一段在幹嘛**。它是那支影片的第一個畫面。踩過兩次：一支影片開在「叫它寫個小工具」那種支線示範上，另一支開在「手把手操作」這種沒說在做什麼的標題上。發現這種情形，改的是單元邊界或那一頁的標題，不是加封面。

### B-3 投影片內文不要寫絕對頁碼或頁數

`p70 - 73`、「這一段共 14 頁」、「你現在看到的這 86 頁」、「接下來四頁講這個」這類寫法，插頁或拆頁之後全部會失效，而且**畫面右下角一直顯示著正確的「Slide N / 總數」，兩邊對不上比沒寫更糟**。typecheck 也抓不到。

改用不帶數字的說法：「接下來這一段」「整份簡報」。分節頁的路線圖用 `SectionDivider` 的 `weight`，那是相對比例，不是頁數。

### B-4 承接語要指到真的下一頁

「接下來講 X」「下一段會做 Y」這種句子寫下去之前，先確認 X 真的就在下一頁。**搬頁、插頁的時候最容易漏掉的就是這種句子**，它不會報錯，也不在 `check:slides` 的檢查範圍內。

已經犯過兩次：

- `21e3_M2_ProjectTypes` 收尾寫「接下來講怎麼分工」，但分工在四十幾頁之後。
- `32d_M2_Recap` 寫「下一段講怎麼設一個目標」，但下一段是 M3 分工，目標循環在 M4。

改法有兩種，選一種：把句子改成真正的下一頁，或者把它降級成不指定時間的說法（「這件事後面會再回來講」）。

### B-5 同一批內容只開一頁

要新增一頁之前，先搜一次關鍵詞，確認這批內容還沒有別的頁在講。**寧可把既有那頁加厚，不要另開一頁。**

犯過一次：M1 曾經連續三頁講斜線指令（`18_M1_Features`、`11d2_M1_ClaudeFlowCmds`、`11e_M1_ClaudeMenuTabs`），重疊約七成，2026-08-04 併成一頁。現在斜線指令的唯一清單是 `11e`，要補指令加進它的四格，不要再另開一頁。

判準：**兩頁的內容互換位置，讀起來有差別嗎？**沒有就是同一頁。

---

## D. 中文寫作規範

### D-1 標點

- **禁用中文破折號（U+2014，通常連打兩個）**，一律改用「，」「。」「：」或（）括號。此規則有 `.claude/settings.json` 的 PreToolUse hook 把關，但手動編輯不會被攔，仍請遵守。

### D-2 禁用的語感

- **對仗句型**：「不僅是 X，而是 Y」「不只 X，更是 Y」。
- **編造的量化數據**：「92% 精準鎖定」「99% 的機率」「效率提升 300%」。沒有可查證來源的百分比一律刪掉。
- **空洞標語**：「用最低成本發揮最大效益」「駕馭未來的最高權限」「長保不敗的通識內功」。
- **浮誇修辭**：「彈指載入」「秒速掌控」「瞬間秒殺 Bug」「無痛」「一鍵」「多功能戰情室」。
- **過度承諾**：「安全 100%」「成功率極高」「它會自己修好」「指數級成長」。
- **未來預測寫成事實**：「不可逆的趨勢」「這是接下來幾年的基本功」。要講預測就標明是預測。
- **收尾金句**：「答得出這三題，這一段就過了」「規矩靠人記會忘，靠程式擋不會」這種自我滿足的結語。頁面結尾要嘛給具體的下一步，要嘛就不要寫。
- **進度安撫**：「剩下三步，你已經學過兩步」「這題你已經有答案了」「別擔心，前面都講過了」。這類句子講的是學員的狀態，不是內容，讀起來像在拍肩膀。三個具體毛病：它不推進理解；它多半不精確（「學過兩步」其實是三步）；而它常常佔掉一頁唯一的重點位置。要標進度就用流程軌或分節頁的路線圖，那是視覺元件的工作，不要寫成句子。<br>判準：**把這句話刪掉，讀者少知道什麼嗎？**答不出來就刪。
- **課務與操作交代**：「這個練習不用開終端機」「現在可以把電腦打開了」「等一下會發下去」「請按下一頁」「把這一頁截圖起來就夠了」。講者現場說一句就過去的話，印在畫面上只會佔位置，學員回頭翻講義時完全用不到。同一類還有「點擊上方卡片，右側會同步切換」這種操作說明，台下不會去點，講者示範一次就懂。判準同進度安撫那條。
- **後設導覽句**：「上一頁講的是⋯」「接下來看三個例子」「這一頁要講的是⋯」「三頁看完，⋯」「下一段專門講怎麼寫」。它描述的是簡報自己的結構，不是內容。**這是最容易長出來的一類**，2026-08-04 全片掃過一次，光這一類就砍掉四十幾句。多數不必整句刪，刪掉句首那半句、留下後半的實質內容就好（例：「前面學過 API、前端、後端與資料庫，現在把它們放回同一張圖。」整句都是導覽，直接刪；「三頁看完，零件的分類其實只有三種載入時機：⋯」只刪「三頁看完」）。<br>它還跟 B-4 共用同一個風險：搬頁之後，指向「下一頁」「下一段」的句子會安靜地指錯地方，`check:slides` 抓不到。
- **自創比喻**：「不是預設收容所」「像一位隨時待命的副駕駛」。比喻若沒有增加理解就是裝飾，直接講事實。
- 「打造」全片留 1 到 2 處就好。

上面四條（收尾金句、進度安撫、課務交代、後設導覽句）共用同一個判準：**把這句話刪掉，讀者少知道什麼嗎？**答不出來就刪。畫面上只留讀者回頭看講義時需要的內容，其餘交給講者口頭補。

#### 這一章有兩層檢查，因為光靠自律擋不住

D 章禁的東西有個共同特徵：**它們讀起來像好句子**。不好的寫法不需要立規則擋。所以寫的當下不會覺得自己在違規，要靠事後檢查才抓得到。

實測的證據：D-1 的破折號有 `.claude/settings.json` 的 hook 擋著，從來沒被違反過；沒有檢查的那幾類一直在長回來。而且長的位置有規律，幾乎都在**開頭句與結尾句**，因為那兩個位置沒有資訊要傳達，沒內容可放的時候就會拿語氣去填。

兩層：

| 層 | 工具 | 抓什麼 |
|---|---|---|
| 字面 | `npm run check:words` | D-3 中國用語、D-2 浮誇詞與過度承諾、D-4 譯名。固定詞表，查得到就是錯 |
| 語意 | `copy-reviewer` subagent | 收尾金句、進度安撫、課務交代、後設導覽句、自創比喻。要看語意，字面抓不到 |

`check:words` 誤報的時候不要把詞從表裡拿掉，那會讓整支腳本失效。兩種處理方式：中文沒有詞界造成的切錯（例如「補上手腳」切出「上手」）加 `skipIf`；整份都在示範壞寫法的頁面（例如 `harness/14_WriteExample.tsx`）在檔案裡加一行 `check-words-ignore-file`。

### D-3 中國用語

| 不用 | 改用 |
|---|---|
| 一鍵 | 依實際操作描述（若是組合鍵，「一鍵」語意本身就錯） |
| 上手 | 熟悉 |
| 技術棧 | 技術堆疊 |
| 調用 | 呼叫（但「呼叫 AI」多半該寫「使用 AI」） |
| 高併發 | 平行處理 |
| 依賴套件 | 相依套件 |
| 本地／本地端 | 本機 |
| 運行 | 執行、跑（「運作原理」這種詞組不受影響） |
| 場景 | 情境 |
| 加載 | 載入 |
| 開箱即用 | 現成可用、不用自己架 |
| 痛點 | 問題、卡住的地方 |
| 土炮 | 自己摸索、自己土法做 |
| 質變／量變 | 直接講清楚變化是什麼 |

**判定原則**：台灣也在用的詞不算中國用語，不要為了「看起來更正式」去改。以下是常見的誤判，維持原樣即可：

| 常被誤判 | 判定 |
|---|---|
| 打開、點擊 | 台灣日常與正式文件都在用。「開啟」「點選」只是文體偏好，本專案走白話路線，維持口語 |
| 通過 | 「通過測試」「通過 Code Review」是正確用法。只有當它等於「經由」時才要改（例：資料通過網路送出 → 經由網路送出） |
| 日誌 | 台灣技術圈通用。本專案的「航行日誌」是刻意的比喻設計 |
| 配置 | 「配置記憶體」是 memory allocation 的標準譯法 |
| 在線上 | 台灣用法，意思是已經上線 |

### D-4 譯名統一

| 原文 | 統一譯法 | 不要用 |
|---|---|---|
| Subagent | 子代理（Subagent） | 小幫手、子層小幫手、側任務專員、專業側任務專員、Sub Agent |
| Orchestrator | 指揮者 | AI 專案經理 |
| Harness | 運作框架（Harness） | 安全沙箱（sandbox 才是沙箱） |
| Agentic Engineering | 代理工程（Agentic Engineering） | Agent Engineering |
| Context Engineering | 上下文工程 | 語境工程 |

大小寫固定寫法：`CLAUDE.md`（全大寫）、`claude.ai`（全小寫）、`Claude Code`、`MCP`、`Skills`。

課程結構的三個詞（章節、單元、階段）各指一件事，混用會讓學員對不回播放器，定義見 B-0b。

#### 「專案」與「Project」是兩個東西

同一個詞在這份簡報裡有兩個意思，混著寫學員會分不出來：

| 指的是 | 寫法 | 例子 |
|---|---|---|
| 泛指的工作單位 | 專案 | 「『專案』不是只有寫程式」、程式專案、專案資料夾 |
| `claude.ai` 的那個功能 | Claude Projects | 「在 Claude Projects 裡」 |
| 那個功能底下的單一個 | 一個 Project | 「一個任務一個 Project」 |

例外是 Claude 自己介面上的中文標籤，照抄不要翻譯，例如「專案知識庫（Project knowledge）」。

### D-5 標題

#### 這一節管的是投影片標題，章節名與單元名走相反的一套

先分清楚要寫的是哪一種，兩者的判準相反：

| | 投影片標題 | 章節名／單元名 |
|---|---|---|
| 寫在哪 | `SlideLayout` 的 `title`、拆頁的 `meta.title` | `SECTION_DEFS` 的 `label`、`UNIT_DEFS` 的 `title` |
| 判準 | 看得出這一頁在講什麼 | 這一段涵蓋哪些主題 |
| 句型 | 不限，清楚優先（見下方） | 名詞短語，必要時「主題：子題」 |
| 例 | 「Git 幫你記下每一版，不只是程式碼」 | 「Git：版本控制」 |
| 例 | 「照你的目標，挑一種 AI 工具」 | 「AI 開發工具的六種類型與選用時機」 |

理由是讀者的處境不同。投影片標題是學員一頁一頁讀過去的，他在**讀**，標題要先讓他知道這一頁會出現什麼。章節名與單元名出現在講者的下拉選單、`npm run units` 印出來的錄製清單、影片檔名與課程平台的目錄，他在那裡是**在找東西**：想知道「Git 在哪一支」「權限模式在哪一支」，能力導向的長句會讓關鍵字掃不到。

章節名與單元名的三個要求：

1. **不要用只有看過投影片才懂的內部說法。**「零件」「旋鈕」「手冊」「每一版都留得住」在課裡都成立，但在選單與目錄上讀者沒有上下文。用學員打得進搜尋框的字：`CLAUDE.md`、Skill、MCP、權限模式、版本控制、部署。<br>反過來也不要無差別代換：單元 5-6 講的正是有些規則不該寫進 `CLAUDE.md`，把「手冊」換成 `CLAUDE.md` 就指錯了。先確認那一段講的到底是哪一個東西。
2. **名稱要涵蓋這一段真正有的東西**，不要拿其中一個單元的名字當整章的名字。
3. **要用到第二個「與」才說得完的時候，先回頭看是不是切點的問題。** 並列的同類項目用頓號列沒問題（「介面、按鍵與指令」）；但一支影片裡裝了兩件不同性質的事，該動的是 `UNIT_DEFS` 的切點，不是名稱。

分節頁的標題是章節名，所以它走這一套，不受本節其餘條文限制。改的時候三個地方要一起改：分節頁的 `title`、`SECTION_DEFS` 的 `label`、還有 `LIVE_TITLES`。對外的課程文案（`課程銷售頁_文案.md` 的章節標題與單元一覽表）也用同一組名字，順手更新。

#### 判準只有一個：看得出這一頁在講什麼

標題的職務是讓讀者在讀內文之前就知道這一頁有什麼。**句型不是判準，清楚才是。** 名詞短語、陳述句、對比句、動作句都可以，挑最快把內容講明白的那一個。

驗收方式：**只看標題，說得出這一頁會出現哪些東西嗎？**說不出來就要改。

| 不夠清楚 | 改成 | 差在哪 |
|---|---|---|
| 你按下按鈕之後，發生了什麼事 | 一次請求的完整流程：前端、後端與資料庫 | 前者只說有事發生，沒說是哪一件 |
| 同一個需求，差別在它碰不碰得到你的檔案 | 同一個需求的三種做法：單次、重複、系統化 | 前者給了比較的軸線，沒說比了哪幾個 |
| 這七樣，各解掉一個會卡住你的問題 | Vibe Coding 需要的軟體基礎知識 | 前者要讀完內文才知道那七樣是什麼類別 |

三種常見的不清楚，病根是同一個：**標題描述的是這一頁的效果或感受，不是它的內容。**

1. **只給軸線不給內容。**「差別在它碰不碰得到你的檔案」說了拿什麼來比，沒說比了哪幾個。
2. **用代稱指內容。**「這七樣」「這三件事」「這四個地方」。數量詞不是內容，把它換成那個類別的名字。
3. **把承諾寫進標題。**「之後才改得動」「一般的聊天對話框都沒有」。那是內文要證明的事，不是標題的工作。

標題可以帶動詞，也可以不帶。「Hook 是程式在擋，不是它記得」比「Hook 的運作方式」清楚，因為它把結論講出來了；反過來「監督與邊界」比「兩個旋鈕：要不要先問你，碰得到多大範圍」清楚，因為後者要讀者先知道「旋鈕」是在比喻什麼。**不要為了讓標題有動詞，把內容換成比喻或狀況描述。**

例外有四種頁：封面、**分節頁**、案例首頁（`案例一：器 VESSEL`）、結語。這四種的職務是開一個名字，不是說明一頁的內容。

分節頁是例外的理由跟另外三種不同，要特別記著：**分節頁的標題就是章節名稱**，它同時是 `SECTION_DEFS` 的 `label`、講者下拉選單的分組名，以及 `npm run units` 印出來的章名，而且課程對外的文案也用同一組名字。改一張分節頁的標題等於改課程結構的對外名稱，不是改一頁文案。真的要改的時候，分節頁標題與 `SECTION_DEFS` 的 `label` 必須一起改，不能只動一邊。

#### 句型照那一頁的職務挑

| 這一頁在做什麼 | 通常最清楚的寫法 | 例 |
|---|---|---|
| 列一組東西 | 名詞短語，把類別名講出來 | 「Claude Code 指令的四種類型」 |
| 教一個結論 | 陳述句，結論直接寫進標題 | 「規則沒生效的三種原因」 |
| 有兩邊要對照 | 對比句 | 「Hook 是程式在擋，不是它記得」 |
| 叫學員動手 | 動作句 | 「動手搭建運作框架」 |
| 引出學員會撞到的狀況 | 情境開場 | 「手機打開，紀錄卻是空的」 |
| 承上啟下的一問 | 提問句 | 「一份手冊該寫多長？」 |

最後兩種最容易寫過頭：它們靠一個具體畫面或一個疑問吸住注意力，代價是標題本身不含主題。**同一節各最多兩頁**，而且那一頁的內文第一句就要把主題補上。

#### 一章裡不要有三個以上標題用同一個句型骨架

清楚優先不代表可以不管節奏。2026-08-22 那一輪為了讓標題「都有動詞」，一口氣改出五個長這樣的：

```
這七樣，   各解掉一個會卡住你的問題
這三塊零件， 你會親手裝一次
四個問題，  問的都是同一份手冊
一個指揮者， 派出執行者與審查者
同一批資料， 擺法決定你之後好不好改
```

每一個單獨看都合規，五個放在一起就是同一個模具印出來的：**數量詞＋名詞，逗號，動詞短語**。這是最容易被認出來的 AI 味，而且它不是選字的問題，是句型重複的問題。

所以改完一批標題之後要多做一步：**把它們抄在一起唸過去**。骨架重複三次以上就換掉其中幾個，換的方式是回頭看那一頁的職務（教觀念、動手、對照、開場），不是換同義詞。

#### 標題不要重複這一頁自己的第一句話

標題與內文的第一段如果講同一件事，等於同一句話講兩次，而且畫面上兩句還會上下相鄰。

2026-08-22 掃過一次，27 個新標題裡有 5 個犯這條，其中一個連口白的第一句都一模一樣（`21f6_M2_ProblemMap`）。

檢查方式：把標題裡連續七個字以上的片段拿去搜同一個檔案的可見文字與 `meta.script`，搜得到就要改。改的是標題還是內文，看哪一個是學員先讀到的：頁標題先讀到，所以通常留標題、改內文（例：`11d_M1_ClaudeShortcuts` 改的是卡片標題，不是頁標題）。

#### 這條規則翻過一次，記著為什麼

2026-08-21 曾經把全片 170 個標題分類，判定其中 34 頁的名詞短語是問題，理由是「規律突然斷掉」，於是 2026-08-22 用「標題要有主要動詞，主詞是學員的能力」改掉 26 頁。2026-08-23 推翻這個方向，那 26 頁裡有 12 頁改了回去或重寫成主題名稱，其餘的照新判準逐頁重看。

翻案的理由不是句型偏好，是那一輪換掉的不只是句型：

- 「如何閱讀 API 文件」→「一份 API 文件，先找這四個地方」：從說明主題變成說明數量。
- 「三個角色：指揮者、執行者、審查者」→「你只跟指揮者說話，另外兩個由它派」：三個角色的名字從標題上消失了。
- 「監督與邊界」→「兩個旋鈕：要不要先問你，碰得到多大範圍」：讀者得先懂「旋鈕」這個比喻才讀得懂標題。

共同的代價是**標題不再說得出這一頁有什麼**，換到的只是句型一致。而且那一輪自己長出兩個副作用（句型模具、標題重複內文第一句），上面兩節就是為了擋那兩個寫的。

所以現在的順序是：清楚第一，句型跟著內容走。句型一致性只有在同一節連續好幾頁時才值得顧慮。

#### 標題改了，記得同步兩個地方

- `src/App.tsx` 的 `LIVE_TITLES`，那是下拉選單用的副本，兩邊必須一字不差。
- `src/courseUnits.ts` 的 `anchor`，如果那一頁剛好是某個錄製單元的起點。

漏了任何一個，`npm run check:slides` 會報。
