# 交接：目前狀態與後續

最後更新：2026-08-04　　commit `106f165` 之後

---

## 〇、2026-08-04 這一輪改了什麼（全片審視）

頁數維持 **151 頁**（新增 2 頁、合併掉 2 頁）。

**新增（M4，Slide 144、145）**
- `28a_M4_LoopPractice`「讓計時器自己跑完一輪」：一段可以整段照抄的 prompt，拆成目標／什麼叫做完／怎麼驗／邊界四塊，右邊標出每一塊來自前面哪一頁。
- `28a2_M4_LoopWatch`「它自己跑的時候，你在旁邊看什麼」：三輪的逐題通過與失敗，加上「你只在三個地方出手」。
- 原因：M4 從頭到尾只講循環的概念與零件，學員沒看過一段真的會自己跑下一輪的指令長什麼樣，下一頁就跳回收作品了。
- 驗證方式刻意用 `webapp-testing` 而不是 `npm run lint`：第一單元的計時器是單一個 `index.html`，沒有 `package.json`，寫 lint 會變成跑不起來的假例子。

**合併（M1）**
- 刪掉 `18_M1_Features`（原「新手友善內建功能」）與 `11d2_M1_ClaudeFlowCmds`（原「對話與會話控制命令」）。這兩頁與 `11e_M1_ClaudeMenuTabs` 講的是同一批斜線指令，重疊約七成。
- `/usage`、`/init`、`/btw` 併進 `11e` 的四格，`/compact` 有損的提醒放在該頁底部；`Ctrl + V` 貼圖併進 `11d` 成為第五個按鍵。
- **之後要補斜線指令，加進 `11e` 對應的那一格，不要再另開一頁。**

**設計規範**
- 清掉全片的常駐無限動畫（A-3）。剩下的三處都是狀態驅動的暫時動畫（`animState`、`isTyping`），不是常駐。
- 表達「系統正在運轉」的慢速動態各段只留一組：Slide 25 資料庫、Slide 129 迴圈圖、Slide 151 結語的兩圈虛線環。
- 清掉色票外的色相：violet、purple、teal、cyan、pink、yellow、blue、green（`orange` 沒有清掉，它後來被正式收進色票，見下一段）。
- 清掉「顏色當編號用」：Slide 3 三大資產、Slide 8 工具六格、Slide 29 工作循環四格、Slide 50 九宮格 badge、Slide 131 三階段、Slide 130 三種債、Slide 132 三個 Skill。

**色票新增 `orange`：Claude 專有名詞**

原本 `orange` 只剩兩處（`11b` 的終端機仿真、`32` 的生態系對照），照舊規範算違規但語意成立。這一輪把它正式收進色票，**規範寫在 `CLAUDE.md` A-1 的「`orange` 的職務」小節**。

- 職務是**標身分，不標重點**：斜線指令、Claude Code 的按鍵、`CLAUDE.md`、`SKILL.md`、Skill 名稱。
- 因為標的是名詞不是焦點，**它不計入「同一頁最多兩種強調色」的額度**，跟灰階同一層級。
- 判準：把那個 token 換成別家工具的對應物，句子還成立嗎？成立就不是專有名詞，維持灰階。所以 `git commit`、`npm run lint`、`.env`、檔案路徑都不上橘，`Shift + Tab` 上橘（那是 Claude Code 定義的操作）。
- 已套用：`11d`（五個按鍵）、`11e`（全部指令 token）、`21b`、`21b3`（`/context`、`/memory`）、`27b7`（Skill 名稱與 Grill Me 卡，原本用 amber，改橘之後 amber 回去只管警告）、`_HandbookPeek`（右下角那顆 `CLAUDE.md` 按鈕）。加上原有的 `11b`、`32`，共 8 個檔案、85 處。
- **不要拿它當重點色**。同一頁如果橘字比 sky 還多，代表用錯了。

**連貫性**
- `07_Div_Terminal`（M1）與 `19_Div_Harness`（M2）補上路線圖，四個分節頁的格式才一致。
- `09b_M1_TopicsOverview` 的前置觀念改成八站，順序與實際播放順序對齊（原本六站，漏掉「看懂紅字」與「上線部署」，而且順序是錯的）。
- `21e3_M2_ProjectTypes` 結尾原本寫「接下來講怎麼分工」，但分工在四十幾頁之後，改成指向下一頁的兩份真實手冊。
- `26_Div_Loop` 路線圖補上「實際跑一輪」那一塊。
- `27b2_M4_GoalLoop` 的 Plan／Act／Test／Review 補上一句，指回第一單元的「探索、計畫、執行、驗證」。
- `27b8_M4_MissionLog` 開頭原本寫「最後兩頁」，是會漂的頁數說法，已改掉（見 B-3）。

**學員卡點**
- `28_M4_Safety` 的 `deny` 設定補上檔案位置 `.claude/settings.json`，並給一句可以直接講的話。
- `25b_M3_HandsOn` 補上「不用自己開資料夾，打 `/agents`」。

**`CLAUDE.md` 自己也做了一次健檢**

- 修掉四個事實錯誤：orange 那節寫「只有兩種用法」卻列三條；B-0 寫死「拆成 56 頁」（實際 48，已改成叫人去跑 `check:slides`）；indigo 對照表把兩頁違規當範例；漏列 `31_Cheat_Dials`。
- 補三條漏掉的規則：禁用色相清單補上 `blue`／`green`／`gray` 系（附一行可以直接跑的掃描指令）；A-4 補「不存在的 animate class」（`animate-spin-slow` 這類靜默失效）；A-4 的提交前檢查從只講 `lint` 改成三支都要跑。
- 新增 **B-4 承接語要指到真的下一頁**、**B-5 同一批內容只開一頁**，兩條都附上這一輪犯過的實例。
- **C 章（Remotion）搬到 `src/remotion/CLAUDE.md`**，根目錄那份從 291 行降到 264 行。理由就是簡報 Slide 65 講的：只在某一區適用的規則放子目錄，碰到才載入。根目錄的表格改成指路。`AGENTS.md` 是 `CLAUDE.md` 的 symlink，會自動跟著。

**刪掉七個孤兒檔**

全部是 git 已追蹤的檔案。刪除還沒 commit 的話用 `git checkout HEAD -- <路徑>` 救回，已經 commit 之後才是 `git checkout HEAD~1 -- <路徑>`。

| 檔案 | 為什麼刪 |
|---|---|
| `src/slides/18_M1_Features.tsx` | 這一輪併進 `11e`，見上面「合併（M1）」 |
| `src/slides/11d2_M1_ClaudeFlowCmds.tsx` | 同上 |
| `src/slides/21d_M2_Extensions.tsx` | 舊版擴充機制頁，已被 `21d1`／`21d2`／`21d3` 三頁取代 |
| `src/slides/21g_M2_ClaudeProjects.tsx` | 舊版 Projects 頁，已被預錄頁取代 |
| `src/slides/21h_M2_ClaudeProjects_Examples.tsx` | 同上 |
| `src/slides/21i_M2_ClaudeCowork.tsx` | 同上 |
| `src/components/Citation.tsx` | 沒有任何頁面引用 |

**副作用：A-1 的色相掃描現在完全乾淨了**，只印出七個色相。`21d` 裡那個 `purple-400` 是唯一的誤報來源，規範裡的但書也一起拿掉了，改成「多印任何一個就是違規，不會有例外」。

保留但目前沒人用的還有 `src/components/RemotionPlayer.tsx` 與 `src/remotion/` 整個目錄，那是刻意保留的，規範在 `src/remotion/CLAUDE.md`。

另外刪掉 `審稿報告.md`（2026-07-20，審的是 76 頁那一版）。**那份沒有進版控，是永久刪除。**它已經沒有任何檔案引用，第五節的內部文件清單裡也沒列它。另外兩份審稿報告留著：`審稿報告_全片_預錄課.md` 裡有還沒執行的 12 處標題改寫，`審稿報告_M2_預錄課.md` 是預錄課字級與字數判準的出處。

**不要刪的八個佔位檔**：`21c`、`21f3`、`21f4`、`21f5`、`21h2`、`32b`、`32c`、`32d`。它們被 `REPLACEMENTS` 蓋掉、畫面上看不到，但還排在 `LIVE_SLIDES` 裡撐 index，刪掉 `App.tsx` 就會壞。判斷孤兒的時候要看的是「有沒有被 import」，不是「有沒有被看到」。

---

## 一、現在是什麼狀態

**簡報 151 頁**，線上網址 https://citysite102.github.io/aionduty-vibe-coding/

實體課日期是 **8/9（日）**，現場用的就是這一份，不是另外一份。

### 拆頁的架構（很重要，先看這個）

現行版與預錄版**共用同一份簡報**，用「漸進式替換」串起來：

- `src/slides/`：現行版頁面（111 檔），還沒拆的部分照舊
- `src/slides-recorded/`：拆好的頁面
- `src/slides-recorded/registry.ts` 的 `REPLACEMENTS`：以「原始頁的 index」為 key，把那一頁替換成拆出來的數頁

目前替換表：

| LIVE index | 原始標題 | 拆成幾頁 | 合併後位置 |
|---|---|---|---|
| 56 | CLAUDE.md 的分層 | 6 | Slide 57 到 62 |
| 66 | 規則明明寫了，它卻沒照做 | 4 | Slide 72 到 75 |
| 67 | 這條規則該放哪 | 7 | Slide 76 到 82 |
| 68 | 手冊越寫越肥，怎麼整理 | 7 | Slide 83 到 89 |
| 69 | 怎麼把話講對：白名單與探索空間 | 7 | Slide 90 到 96 |
| 70 | 同一套手冊，換個地方用 | 7 | Slide 97 到 103 |
| 73 | 換成你的工作，手冊該寫什麼 | 9 | Slide 106 到 114 |
| 74 | 這一段你完成了三件事 | 1 | Slide 115 |

111 − 8 + 48 = **151 頁**。拆得越多，總頁數越接近目標的 265 頁。

**`REPLACEMENTS` 的 key 是 `LIVE_SLIDES` 的 index，所以在現行版插一頁，插入點之後的 key 全部要往後推一格，`SECTION_DEFS` 的 start 也是；刪一頁就往前推一格。** 2026-08-04 刪掉那兩頁重複的指令頁時，八個 key 與三個 start 都推過 −2。

改完不要用手算，跑 `npm run check:slides`，它會核對 key 上面的註解標題與 `LIVE_TITLES[key]` 對不對得上。要看合併後的完整順序，跑這段：

```bash
node -e "
const s=require('fs').readFileSync('src/slides-recorded/registry.ts','utf8');
const body=s.match(/REPLACEMENTS: Record<number, RecordedSlide\[\]> = \{([\s\S]*)\n\};/)[1];
const re=/^\s{2}(\d+): \[([\s\S]*?)\n\s{2}\],/gm; const reps={}; let m;
while((m=re.exec(body))) reps[+m[1]]=m[2].split('\n').filter(x=>x.includes('Component:')).length;
let idx=0; for(let i=0;i<111;i++){ if(reps[i]){ console.log('LIVE index',i,'→ Slide',idx+1,'到',idx+reps[i]); idx+=reps[i]; } else idx++; }
console.log('總頁數', idx);"
```

`src/slides-recorded/harness/` 的檔名編號是**建立順序，不是播放順序**。播放順序只由 `registry.ts` 決定，所以新拆的頁一律往後編號就好，不用為了插在中間去重編既有檔案。

**分節切點會自動換算**，`App.tsx` 的 `SECTION_DEFS` 只記錄每一節起點的「原始頁 index」，拆頁造成的位移由程式計算。以後拆頁不必再手動改任何切點數字。

### 常用指令

```bash
npm run dev        # 本機預覽（3001 被佔用時會自動跳 3002）
npm run lint       # tsc --noEmit
npm run check:rec  # 檢查拆好的頁面：字數、寫死字級、meta、口白長度
npm run build      # 建置
```

部署：推上 `main` 就會自動跑 GitHub Actions 部署。

錄製時用 `?clean=1` 隱藏右下角操作列。

---

## 二、已經定案的規格（不要重新討論）

完整規格在 `預錄版拆頁設計.md`，這裡列最常用到的：

1. **單頁中文 160 字上限**。這是唯一能自動檢查的硬約束，`check:rec` 會擋
2. **一頁一個主張**。標題有「與」「和」「以及」就是兩頁
3. **標題問問題，內文回答**。同系列共用一個問句當標題，各自的答案放內文第一行，並配一組編號（`原因 2 / 3`、`技巧 4 / 6`）
4. **一頁只有一個白色粗體**，就是那個答案。問句用 `text-2xl text-slate-300` 不加粗，關鍵字用 sky
5. **不要用寫死的 `text-[NNpx]`**，縮放控制對它無效
6. **共用同一份 `SlideLayout`**，不另做預錄版的 Layout
7. 每頁 `meta` 要有 `id`、`title`、`script`（口白稿）、`seconds`、`from`

### 已建立的共用元件

- `_RouteRail.tsx`：四問流程軌
- `_HealthRail.tsx`：五步健檢流程軌
- `_LayerRail.tsx`：CLAUDE.md 四個放置位置
- `_SurfaceRail.tsx`：Projects／Cowork／Claude Code 三個地方
- `_DontDo.tsx`：紅綠正反對照（沿用現行版 32b 的設計）

四條軌的結構一樣，只有 `STEPS` 陣列不同。要再開一條就複製一份改字串，不要把它們合成一個泛用元件，各段的間距與寬度會不一樣。

---

## 三、下一步

依 `逐頁拆頁評估.md` 的建議順序：

1. **M2 剩下的頁**，優先挑「照節拍切」那批（`stepIndex` 數量已經夠）
2. **M4 循環工程**（標題品質最好，改動最少）
3. **解構 Vibe Coding**
4. **課前導讀與結語**
5. **M1 終端機實作**，其中 Slide 25「前端與後端的 API 互動與串接」（1,366 字、只有 4 個節拍）需要重新設計而不是拆解，留到最後

`逐頁拆頁評估.md` 點名的 M2 驗證批（68、69、70、76、56、73、77）已經全部拆完，加上 71 共 8 頁、53 頁產出。M2 還沒拆的頁裡，這幾頁節拍數足夠，可以直接接著切：

| 原 Slide | 標題 | 節拍 |
|---|---|---|
| 47 | 為什麼你該花錢買 AI 算力？ | 5 |
| 52 | 動手搭建運作框架 | 5 |
| 53 | 跟著做：寫出第一份 CLAUDE.md | 5 |
| 54 | 怎麼確認它真的讀到了 | 5 |
| 55 | 這三種寫法，寫了等於沒寫 | 5 |
| 59 | 真要擋住，就不要放進 context | 5 |
| 63 | 協作角色拆解 | 5 |

53、54、55 跟已經拆好的分層那十頁是同一條敘事線（寫出來 → 確認讀到 → 避免寫成廢話 → 分層），建議一起做完再往下。

### 還沒做的其他事

- 全片 253 處寫死的 px 字級換成 rem class（換完縮放控制才會對整份生效，但要逐頁確認有沒有溢出，**建議 8/9 之後再做**）
- 其他四個區塊的鉤子頁與收斂頁（照 `28b_M4_FirstDay` 的規格）
- 12 處名詞標籤標題改寫，清單在 `審稿報告_全片_預錄課.md`
- ~~三個孤兒檔案已無引用，未刪除~~ **2026-08-04 已刪**，連同另外四個，見第〇節
- `09_M1_CodeIntro` 與 `13_M1_Example2` 用 `indigo` 當單一強調（對照的另一邊是灰階不是 `sky`），照 A-1 的判準該改成 `sky`。已記在 `CLAUDE.md` 的 indigo 表底下
- ~~`CLAUDE.md` B 章的插頁五步驟過時~~ **這條之前記錯了**。自動換算的是下拉選單的 slice 範圍，`SECTION_DEFS` 的 `start` 還是要手動推。2026-08-04 刪兩頁時三個 start 都推過 −2，B-1 第 5 步是對的，不要刪

---

## 四、審查與寫作上反覆出現的問題

這幾類問題在這一輪被抓到多次，寫在這裡避免重蹈：

- **假例子**。曾經用「註解一律寫中文」跟「不要留廢話註解」示範規則衝突，但這兩條並不衝突。範例必須真的成立，最好取自本專案的 `CLAUDE.md`
- **標題涵蓋不準**。曾經寫「有兩種情況，它根本沒看到」，但其中一種是有讀到、只是份量不足
- **抽象比喻與術語**。「只是拜託」「不佔位」「共通點是延後」「不是預設收容所」都被指出過。改成直接描述機制
- **收尾金句**。「答得出這三題，這一段就過了」這種自我滿足的結語。頁面結尾要嘛給具體下一步，要嘛不寫
- **絕對頁碼與頁數會漂**。2026-08-02 做過一次全片連貫性檢查，抓到分節頁的路線圖寫著 `p70 - 73`、Slide 67 寫著「這 86 頁」，兩個都是 86 頁那一版留下來的化石，而右下角同時顯示「Slide 123 / 141」。規則寫進 `CLAUDE.md` B-1，投影片內文一律不要出現頁碼或頁數
- **貫串全片的設定要對得上**。`mission-timer` 的航太語彙固定是「發射、待機、返航、補給」，出現在 Slide 52、53、65 與拆頁後的 95。前三個是 M1 就有的按鈕，補給是 M2 驗收時新加的那一個，所以 Slide 36（`12_M1_Example1`）維持三個按鈕是對的。改任何一處都要四處一起改，否則 56、57 的對照會失去出處
- **中國用語**。使用者最在意這一項，規範在 `CLAUDE.md` D-3，含一張「常被誤判」表（打開、點擊、通過、日誌、配置是台灣通用詞，不要過度修正）

---

## 五、內部文件（已 gitignore，不會上線）

- `邀請文_20260809.md`：三個版本的招生文案
- `報名問卷_20260809.md`：20 題報名表設計
- `Banner_Prompts_20260809.md`：主視覺的生圖 prompt
- `審稿報告_M2_預錄課.md`、`審稿報告_全片_預錄課.md`：課程審查報告

會上線的講義在 `public/handouts/`：CLAUDE.md 模板、規則分流判斷卡、五步健檢檢查表、錯誤訊息判讀對照表，另有 A4 四頁的列印版。
