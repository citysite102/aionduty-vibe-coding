# 逐頁審稿報告

審查範圍：76 張投影片（依 App.tsx 播放順序）＋ src/remotion/ 動畫元件
審查面向：教學結構、內容正確性、用詞（中國用語／AI 浮誇用語）
日期：2026-07-20

---

## 零、總結論

| 面向 | 評價 |
|---|---|
| 中國用語 | **幾乎沒有**。全域機械掃描 0 命中，人工複審僅「上手」「技術棧」「一鍵」等零星詞 |
| AI 浮誇用語 | 集中在少數頁（11b、11d、27、27b5、32、33_Outro），約 30 處 |
| 內容正確性 | **最大問題所在**。約 20 處明確事實錯誤，其中 6 處會讓學員照做失敗 |
| 教學結構 | 名詞先用後定義、同一論點重複三次、單頁過載，是三個系統性問題 |

---

## 一、P0：照做會失敗／明確錯誤（務必修）

| # | 位置 | 問題 | 建議 |
|---|---|---|---|
| 1 | `11e_M1_ClaudeMenuTabs.tsx` 整頁 | `/help` 的「四大分頁 UI」為虛構；權限模式三個名稱（Not-allowed / Custom-confirmation / Auto-approve）Claude Code 中都不存在；`/config` 的「快取加速設定」不存在；`/deep-research` 被當成官方內建（實為自行安裝的 skill）。至少 7 處錯誤，且與 11d 自相矛盾 | **整頁重寫或刪除** |
| 2 | `25b_M3_HandsOn.tsx:19-22, 41-45` | subagent 檔案範例只有結尾 `---`，缺開頭 `---`。學員照抄 frontmatter 會失敗 | 補起始 `---` |
| 3 | `31_Cheat_Dials.tsx:69` | 把 `acceptEdits` 的行為說成「Claude Code 預設的最推薦狀態」 | default 模式編輯前會徵求同意；改為 acceptEdits |
| 4 | `30_Cheat_Perms.tsx:221` | 「預設模式每次**讀寫**都會確認」錯誤 | default 下讀取類工具不需確認 |
| 5 | `30_Cheat_Perms.tsx:271` | 把 `console.anthropic.com/settings` 標為 Claude Code 設定頁 | 那是 API 開發者主控台；改 `.claude/settings.json` |
| 6 | `17_M1_Error.tsx:117` | `@/components/ui/dialog` 是專案內路徑別名，卻解釋成「套件未安裝，用 CLI 安裝」 | 真正原因：檔案不存在，或 tsconfig `paths` / vite `alias` 未設 |
| 7 | `32_Cheat_Tools.tsx:75-81` | `/agents "任務字串"` 語法錯誤（`/agents` 是管理介面）；「子沙箱」說法錯誤（subagent 無隔離執行環境） | 改為建立 subagent 後以自然語言指名 |
| 8 | `21f_M2_ClaudeMdRealWorld.tsx:55-56` | `@db-migration` 語法錯（`@` 是檔案 mention）；把 GitHub MCP 講成 Skill，且與同段 54 行自相矛盾 | 改「請使用 db-migration Skill」／「透過 GitHub MCP」 |
| 9 | `27b2_M4_GoalLoop.tsx` 整頁 | 整頁立論建立在 `/goal`、`/loop` 的官方定位，以及「OpenAI CLI 2026 上線 /goal」上，皆**未經查證**且寫成確定事實 | 上台前務必查證；建議改寫成不綁指令名的「目標＋停止條件」原則 |
| 10 | `27b5_M4_NoCodeBridge.tsx:143` | 「99% 的機率能定位白畫面真兇」為捏造數據 | 刪除百分比 |
| 11 | `09_M1_CodeIntro.tsx:138` | Bun 案例數據不符公開資料：實為 Zig→Rust 重寫、約百萬行（非 75 萬）、11 天、既有測試全通過；查無「99.8%」 | 改正數據；建議加註社群對該案例的品質爭議 |
| 12 | `03_Intro_Vibe.tsx:20-21` | 「有 review code 就不叫 vibe coding」是 **Simon Willison**（2025/03）的定義，誤植給 Karpathy | 改正出處，Karpathy 為 2025/02 造詞 |
| 13 | `10_M1_TerminalBasics.tsx:105-115` | 虛構 `mkdir` 的成功訊息（真實 mkdir 沉默即成功）；`ls` 模擬輸出與右側目錄樹矛盾 | 改為無輸出，並教「沒紅字就是成功」 |
| 14 | `10c_M1_WebArch.tsx:53` | 把「Token 存 localStorage」標榜為「安全儲存」 | localStorage 存 JWT 易遭 XSS；註明教學示意，正式用 HttpOnly Cookie |
| 15 | `27b4b_M4_Caveats.tsx:87` | 「可設定單次任務不得超過 $2」——Claude Code 無此內建設定 | 改為 API 用量上限／自寫腳本監控 |
| 16 | `21_M2_Pillars.tsx:50` | 「系統**定時**清理對話記憶」錯誤 | compaction 是逼近上限時自動觸發或手動 `/compact`，且**有損** |
| 17 | `13_M1_Example2.tsx:25` | 教新手讓 AI「直接執行」批次改檔名（不可逆操作），無防護 | 改「先印出對照表讓我確認再執行」 |
| 18 | `13_M1_Example2.tsx:49,61-63` | 輸入含 .png/.jpeg，卻要求全部輸出 `.jpg`，副檔名與實際格式不符 | 改「保留原始副檔名」 |

## 二、P1：過時資訊（半年內會被打臉）

- **模型名稱**：`11b:34,124` Sonnet 4.5、`10b2:84` claude-sonnet-4-5、`11c:93` GPT-4o、`27:212` o1 → 現行為 Claude 5 系列 / Opus 4.8 / Haiku 4.5
- **版號**：`11b:85` v2.0.0 → 實測 2.1.215
- **文件網址**：`11b:186`、`10b2:62,71`、`21c:69` 的 `docs.anthropic.com` → 現為 `docs.claude.com`
- **CLAUDE.local.md**（`21c:50-58`）已 deprecated；`21c:66` 的 AGENTS.md symlink 做法可能已不需要
- **安裝方式**（`11:34`）：官方首選已改為原生安裝器，npm 為次選
- **`/cost`**（`18:23`）已是 `/usage` 的別名
- `32:40` `.cursorrules` 已 deprecated；`32:83` OpenAI Swarm 已停止維護；`32:107` Devin/Cursor 被誤歸「開源陣營」
- `21j:26` 「Claude Code 是純文字、無法做 Artifacts」已過時
- `11c:79,85` Cursor 定價與 BYOK 需重新查證

## 三、P1：內容缺口（最該補的東西）

1. **`28_M4_Safety.tsx` 掛著 Safety 招牌卻沒有實質安全內容**。缺 bypassPermissions 風險、API key/.env 外洩、prompt injection（無人值守 loop 下風險最高）、對外動作的人類閘門。
2. **`30_Cheat_Perms.tsx` 沒有 `settings.json` 的 `permissions.allow` / `deny` 實際寫法** — 這是學員唯一能帶回去套用的東西。
3. **Subagent 的關鍵機制事實全簡報從未交代**：獨立 context、只回傳摘要、彼此無法直接通訊。`24:248` 甚至寫成「成員對等對話」。
4. **Harness 段沒有 context window 實際大小、沒有 prompt caching、沒有 token 單價**，卻叫「Token 經濟學」（19d）。
5. **整組 13 頁沒教 `Esc` 中斷**（新手最重要的煞車鍵）**與 `/model` 切換模型**（控制成本最直接的手段）。
6. **定價頁（11c）缺 Max 5x / 20x**，且沒警示「Pro 額度跑 Claude Code 很快撞上限」——最會誤導學員的一點。
7. **`10e_M1_Git.tsx` 整頁沒有任何一條 git 指令**，也沒提 staging area 與 merge。
8. **`21j` 標題講「整合」卻沒提任何入口**：VS Code / JetBrains 擴充、GitHub Actions、桌面版、claude.ai/code。
9. **完全未交代 macOS vs Windows 指令差異**（`ls`/`pwd` 在 Windows cmd 不存在）。
10. **MCP / Skills / Subagents / Hooks 四者分野**始終沒講清楚（19c、21a 各講一半）。

## 四、結構問題

### 4.1 名詞先用後定義（依嚴重度）
- `04b` 第 8 頁就丟出 Query/Key 向量、Attention、Entropy、Cosine Similarity，並預設懂 shadcn/React/Tkinter — 對照 09b 才開始教「前端後端」，嚴重超前
- `10c/10c2` 密集使用「Postgres」「SQL」「索引」，但 10d 才定義資料庫 → **建議 10d 移到 10c 之前**
- `17_M1_Error` 範例用 Vite / npm / 路徑別名，但此刻連 `cd`、`npm` 都沒教 → 換成終端機層級的錯誤
- `19b` 已把 Agent Skills 當解法講完，但 21a 才定義
- `11b:34` 用「Max 20x」但定價頁在 6 頁後且沒提 Max
- `10b:50` 首次出現 `200 OK` 毫無解釋，下一頁就用 POST/GET/201

### 4.2 同一論點重複
- 「該用網頁對話還是終端機」在 `04`、`05`、`09` 講三次 → 三選一
- Harness 在 `19c`（六元件）、`20`（軌道比喻）、`21`（三支柱）三套分類法並存 → 統一以三支柱為主線
- 「理解債」在 `27b4:22` 與 `28:50` 各定義一次
- 「變成自己專案的局外人／陌生人」比喻出現三次（27b4、27b4b、28）
- `/compact` 在 `18:32`、`11d2:38`、`11d2:62`、`11e:40` 出現四次
- 熔斷／防護三點在 `27b4b` 與 `28:37-41` 高度重疊
- `04b2:43,50` 的例子與 `04b:14,27` 逐字相同
- `10c2` 的 Request/Response 與 `10c` 逐字重複

### 4.3 順序建議調整
- `15_M1_Workflow`（探索→計畫→執行→驗證）是理解全部操作的心智模型，卻排在最後 → **前移到 11 之前**
- `27b4_Vocabulary` 詞彙表位置太晚 → 前移到 27 之後
- `27c_M4_Scale`（價值）排在 27b4b（風險）之後，情緒曲線「讚頌→警告→又讚頌」→ 移到 27b4b 之前
- `27b5`（Linting／白畫面除錯）屬基礎工具知識，卻排在「風險與熔斷」之後 → 前移到 25b 附近
- `31`→`30` 播放順序與檔名數字遞減相反 → 改名為 21d_/21e_，並考慮升格為「第四根支柱：邊界」
- `04` 工具光譜夾在 03b 與 04c 之間，把同一主題切開 → 改為 `03 → 03b → 04c → 04`
- `09b` 基礎地圖移到 `09` 之前

### 4.4 單頁過載（最小字級被壓到 text-[11px]）
`02_Philosophy`、`04b`、`09`、`10_TerminalBasics`（12 個知識點）、`10c`（8 個未定義專有名詞）、`11e`（16 個資訊點）、`11c2`、`20`、`24`、`27`、`31`（3×3 九宮格）

### 4.5 測驗設計
`18b_M1_Quiz` 答案與題目同時出現（沒有思考空隙）；五題全考選型，**本段教的安裝／快捷鍵／指令／定價／邊界一題都沒考到**；情境四答案（用 AI Studio 打草稿）與題幹「高度客製化」矛盾。

### 4.6 程式碼層面
- `border-slate-850` 非 Tailwind 內建色階，邊框實際不渲染 — 出現在 `11d:35`、`11d2:36,88`、`11e:121`、`11c:38,54,75,91`、`11c2:289,423`、`21g:22`、`27b3:134,139,146` 等十餘處 → 統一改 `slate-800`
- `27b2:74` `py-0.2` 非合法 class
- `27:10-15` 用 `setInterval` 每 8 秒自動切 tab，講者講到一半畫面會自己跳走
- `33_Outro:75-82`、`27b6:120-122` 是不可點的裝飾按鈕，現場有人點會沒反應
- `11:72`、`21b:55` 背景貼圖來自外部網址，離線播放會破圖
- 未使用的 import：`01:2`、`24:4`、`27b5:1-2`、`27b6:1`
- `17_M1_Error.tsx:5` 元件名為 `Slide09()`，與檔名不符

## 五、用詞

### 5.1 中國用語（僅這些，整體很乾淨）
| 詞 | 位置 | 建議 |
|---|---|---|
| 一鍵 | `10:332`、`10a:56`、`10b2:54`、`17:117`、`11e:51`、`04:65`、`21i:33` | 逐處改寫（`10:332` 的 Ctrl+U 是組合鍵，「一鍵」語意本身就錯） |
| 上手 | `09:142` | 「熟悉」 |
| 技術棧 | `04b:28` | 「技術堆疊」 |
| 呼叫 AI | `21i:27` | 此處指「使用」，屬「調用→呼叫」過度套用 |
| 高併發 | `31:87` | 「平行處理」 |
| 依賴套件 | `05:24` | 與同檔 `:61`「相依套件」不一致 |

### 5.2 AI 浮誇用語
- **「不僅是 X，而是 Y」對仗句型**：`02:14`、`09:116`、`11c2:128`、`33:50-53`、`33:57-58`
- **編造的量化數據**：`04b:28,51`「92%+ 精準鎖定」、`04b2:51`、`27b5:143`「99% 機率」、`31:88,98,107` 效率/風險百分比
- **空洞標語**：`11c:16`「用最低成本發揮最大效益」、`11c2:133`「算力自主 = 效率自由」、`32:185`「長保不敗的通識內功」、`32:285`「駕馭未來任何 Agent 的最高權限！」、`21j:16`「達到最佳效率」、`33:59`
- **浮誇修辭**：`11b:20,23`「精美儀表板／多功能戰情室」、`11d:63,95`「彈指載入／秒速掌控」、`27:278-290`「通關密技／物理超渡」、`27b5:112,115`「救命稻草／瞬間秒殺 Bug」、`11e:36`「精心調校的 Agent 工具箱」、`21g:20`「瞬間化身為該領域的專家」、`10c2:179`「流暢、極速、無痛」、`10e:48`「無痛看懂」、`18:62`「無痛掌握環境」
- **「打造」7 處**：`02:14`、`03:32`、`06:18`、`11:43`、`20:31`、`27b:12`、`33:81`（建議留 1–2 處）
- **過度斷言／過度承諾**：`19d:58`「成功率極高」、`30:119`「安全 100%」、`31:99`「極度高效且安全」、`13:70`「遇到錯誤它會自己修」、`27b5:154`「一行程式都寫不出來也能完美指揮 AI 解決一切」、`27b6:36`「直接輸出極度精準的系統」、`27c:19,66`、`28:19`「指數級成長」、`10b2:30`「乾淨無誤」
- **未來預測寫成事實**：`27b2:126`「不可逆的趨勢」、`27b2:106`「奠定業界新標準」、`27b2:140`「這是接下來幾年的基本功！」

### 5.3 譯名不一致
- Subagent：「子層小幫手」(32:74) / 「側任務專員」(21d:43) / 「專業側任務專員」(21e2:42) / 「Sub Agent」(24) → 統一為「子代理 (Subagent)」
- Orchestrator：「AI 專案經理」(23:22) / 「指揮者」(24)
- `Agent Engineering`(01:69) vs `Agentic Engineering`（全片其餘）
- `Claude.md`(02:75) / `Claude.ai`(21g:20) 大小寫
- `04c` 頁內標題「演進」vs App.tsx 標題「差異」
- Harness 在 `27b4:9-10` 被譯成「安全沙箱」（sandbox 才是沙箱）
- 模組編號：`22` 與 `26` 都標 MODULE 3，但 27 之後檔名全是 M4，全場沒有 MODULE 4

## 六、Remotion 動畫（對照 CLAUDE.md 規範）

✅ 四個元件皆無 `setTimeout`；`theme.ts:16-24` 的 `fadeInMove`（12px + easeOutCubic）完全符合規範；`TwoDials.tsx:63-64` 主色控制良好。

⚠️ 問題：
- **四個元件在 src/ 內沒有任何地方 import，等同死程式碼** → 先確認是否保留
- `theme.ts:10-14` 定義了 `springConfig` 卻幾乎沒被引用，各元件各寫 damping 15/20/30、stiffness 60/100/120 → easing 不統一（違反原則 3）
- `WorkflowLoop:63`、`TwoDials:17`、`OrchestratorSplit:92` 用 CSS `transition` → Remotion 逐格輸出時不生效，預覽與影片不一致，改用 `interpolate`
- `WorkflowLoop:58` 節點每 1.3 秒輪流高亮 → 短於「關鍵元素至少停 1.5–2 秒」
- `OvernightLoop:103,125,143` 引入 theme 外的 `#f59e0b`、`#10b981` → 違反「顏色越少越乾淨」
- `OvernightLoop:18` 用 `frame % 60` 驅動 spring → 每 60 frame 不連續跳變
- `OvernightLoop:28` 每秒轉 450 度 → 過快
- `OvernightLoop:139` 用 CSS `animate-spin` → render 不可靠
- `OrchestratorSplit:37,62-68` 從中央飛到 ±400px → 違反「進場位移 10–16px」
- `OrchestratorSplit:83,86` hardcode 畫布中心 `960` → 解析度一變就錯位

另，投影片本身也有違反動態原則之處：
- `23:13-15,30-32,48-50` 用 `scale: 0→1` + `x: -50` 彈入 → 違反「不要彈跳、不要大幅飛入」
- `27` 單一畫面至少 8 組無限迴圈動畫並行、`33_Outro` 5 組（含 `animate-bounce`）→ 違反「一次只動一個重點」
- `24:223-253` 同頁四種強調色並存 → 違反「主色只給要強調的元素」
- `10a:30,74` 兩個 AnimatedBlock 同為 `stepIndex={2}`；`27b5:109,119` 同為 4；`27b3:96` 左右欄同號

---

## 七、建議修正順序

1. **先修 P0 的 18 條**（尤其 `11e` 整頁重寫、`25b` frontmatter、`27b2` 查證）
2. **補三個內容缺口**：28 頁的真實安全內容、30 頁的 settings.json 寫法、Esc 與 /model
3. **更新所有過時資訊**（模型名、版號、文件網址）
4. **調整順序**：15 前移、10d 前移、27c 前移、詞彙表前移
5. **用詞收斂**：對仗句型、編造數據、空洞標語三類優先
6. **Remotion**：先確認四個元件是否還要，要留就統一 springConfig 並改掉 CSS transition
