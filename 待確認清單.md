# 待確認清單（2026-09-10）

三個檢查 agent 產出，已排除 team-lead 能自行查證的部分。這一份只留**需要你本人確認**的。
已修的 15 處見 commit de644d6，不在這份裡。

---

## A. 開 Claude Code 對畫面就能確認（6 條）

這六條我查不到官方依據，而那幾頁的教學價值整個建立在這些細節上，猜不得。

- [ ] **`/context` 有沒有印每個檔案的 token 數**
  `src/slides/21b3_M2_ContextCheck.tsx:44-50` 的模擬畫面印著 `├ CLAUDE.md　3.7k tokens`。
  若實際沒有那一欄，同頁 READINGS 第 3 條「佔多大？」會落空，學員在畫面上看不到那個數字。
  → 開一次 `/context` 截圖對照。

- [ ] **`/goal` 的五條細節**
  `src/slides/28a3_M4_GoalCommand.tsx` 整頁。指令存在、用途正確，但這五條查不到出處：
  不帶參數看進度／`/goal clear` 收手／一個對話一次只能有一個目標／
  每輪送給「另一個比較小的模型」判定並回三種狀態／沒有內建輪數上限。

- [ ] **Cowork 能不能執行指令**
  `src/slides-recorded/harness/33_SurfaceIntro.tsx`（Slide 119）寫「能讀能寫，不執行指令」。
  這是該表裡 Cowork 與 Claude Code 的**唯一區別**，寫錯整張表的結論就翻掉。

- [ ] **`CLAUDE.local.md` 與子目錄，哪一個蓋過哪一個**
  `harness/26_LayersOverview.tsx`（Slide 69）四層由上到下排：全域→專案根→子目錄→`CLAUDE.local.md`，
  並寫「下面的蓋過上面的」。官方載入順序是 …→專案→專案 local→巢狀目錄，最後兩層與此相反。
  載入順序未必等於覆寫優先序，所以不敢直接判錯。

- [ ] **Hook handler 寫了不支援的型別，是報錯還是安靜失效**
  `harness/66_HookHandler.tsx`（Slide 98）寫「不會報錯，只會安靜地什麼都不做」。
  若實際會報錯，這句反而會讓學員不去看錯誤訊息。

- [ ] **`Shift + Tab` 的循環到底包含哪幾個**
  `30_Cheat_Perms.tsx:39,437`、`11d2_M1_TerminalKeys.tsx:20` 寫「只在 default → acceptEdits → plan 之間循環」。
  兩輪查證都說還包含 `auto`（環境可用時）與 `bypassPermissions`（用該旗標啟動時），只有 `dontAsk` 確實不在循環裡。

---

## B. 沒有出處的宣稱，要補來源或降級說法（3 條）

- [ ] **Anthropic 內部「2~3 週 → 2~3 天」**
  `09a_M1_Benchmarks.tsx:65-74`。同頁 Bun 那組（53 萬行／11 天／64 個代理／$16.5 萬）查得到、與報導一致；
  這一格是全頁唯一沒出處的。CLAUDE.md D-2 禁編造量化數據。

- [ ] **Andrew Kelley 的「未經審查的產出」**
  `09a_M1_Benchmarks.tsx:49`。他確實公開批評過這次改寫，但引號裡這句原話查不到。
  而且 Bun 官方說法是「每個 commit 都經過兩次對抗式審查」，跟「未經審查」正面衝突。
  → 引號拿掉改成轉述，或補原文出處。

- [ ] **`Agent Plugins 1.0.0`**
  `21a_M2_MCPSkills.tsx:126-132`，標 `2026.08`，寫 Amazon／Cursor／Microsoft／OpenAI／Vercel 共同發布，
  且「Claude Code 目前不在支援名單裡」。點名五家又帶版本號，但 agent 與 team-lead 都查不到這個規格。

- [ ] **`npx skills add mattpocock/skills --skill=grill-me`**
  `21a5_M2_PopularSkills.tsx:78`。這個安裝指令與 `grill-me` 這個 Skill 都查不到。
  若是社群第三方 CLI，畫面上要標明不是官方指令，否則學員打不通會以為自己裝錯。

---

## C. 要你決定方向的編排題（4 條）

- [ ] **章節五後半 20 頁講的是章節六的主題**
  兩章合計 84 頁，佔全片 49%。Slide 63-82 講 CLAUDE.md 怎麼寫、規則放哪一層、零件放哪個資料夾，
  正是章節六名字上的主題。連帶 `19_Div_Harness` 的路線圖只列三段，漏掉那十頁。
  選項：(1) 把 `21g_Div_Handbook` 移到 Slide 63 之前，章節名要改（三處同步）
        (2) 只補 `19_Div_Harness` 路線圖第四段
        (3) 不動，接受這是課程重心

- [ ] **Slide 66 `21b4_M2_BadRules` 提前給掉章節六的答案**
  兩個壞例子（「畫面要好看，風格保持一致」「絕對不要刪掉我的檔案」）與收尾判準，
  跟章節六手冊演化的 Slide 103／117／118 幾乎一字不差（已比對 `_handbookVersions.ts` 屬實）。
  學員在 66 拿到答案，72 才看到謎題。同頁還先用了 Hook（Hook 首次解釋在 Slide 75）。
  選項：(1) 整頁刪掉，唯一獨有的「指涉不明」併進 Slide 87（要推 index，風險最高）
        (2) 三個壞例子整組換掉，不碰章節六那三行

- [ ] **四項單元邊界**（各一行 `courseUnits.ts`，但 index 要重算）
  8-3「部署上線」開在「幫計時器加上航行日誌」／5-3「MCP 與 Skills」開在路線圖頁且切斷刻意的承接／
  6-2「Hook」最後三頁在收上一支的線／8-2 一支影片塞四件事

- [ ] **`30_Cheat_Perms` 標題「四個權限模式」**
  內文句子本身是通的，但全部其實有六個。改標題等於改課程用詞。

---

## D. 順手可做，等你點頭（低風險）

- [ ] 刪孤兒檔 `21j_M2_ClaudeCodeIntegration.tsx`（零引用）
- [ ] 刪孤兒檔 `06_Threads.tsx` —— 但 `CLAUDE.md:69` 拿它當 indigo 合法用法的範例，要同時更新那張表
- [ ] `10c_M1_WebArch` 的 `Token`／`JWT Token` 改「登入憑證」／「JWT 憑證」（同頁 :59 已經寫「JWT 憑證」，自己就不一致；且與章節五計費用的 token 撞名）
- [ ] `10c_M1_WebArch` 的 `React 18` 改 19、`Postgres` 從「後端框架」那列移到資料庫
- [ ] `10d_M1_Database:197` 的 `Firebase` 改 `Firestore`（`10c3_M1_Deploy:110` 把 Firebase 當託管服務，兩頁定位不一致）
- [ ] `32_Cheat_Tools` 第四列的 `Composer` 放錯格（它是多檔案編輯介面，接近第三列的「分工與派工」）
- [ ] `11_M1_CodeSetup` STEP 2「讀得到你寫過的規矩」—— 那頁在章節四，學員要到章節五才寫第一份手冊
- [ ] `harness/32_AgentsMd`（Slide 71）的 `ln -s`，Windows 一般帳號會失敗，要補替代做法
- [ ] `21a4_M2_SkillInstall:105-114` 把「子代理」從「要重開才掃得到」的列舉裡拿掉（子代理有檔案監看）
- [ ] `10_M1_TerminalBasics` 五處驚嘆號、兩處人稱「我們」
- [ ] copy 報告的 51 筆文案（AI 語感 33、用詞 8、句型模具 5 組）

---

## E. 已查證確定沒問題（免得之後重複懷疑）

模型單價（Opus 5 輸入 $5／輸出 $25，快取讀取約十分之一）、Pro／Max 方案與 5 小時滾動視窗、
API 示範（`claude-sonnet-5`、`POST /v1/messages`、`x-api-key`、三個必填欄位）、兩個文件網址、
安裝指令（`install.sh`／`install.ps1`／npm）、全部斜線指令的存在與描述、`/rewind` 的限制、
`acceptEdits` 會放行 `mkdir`／`mv`／`cp`、`.claude/rules/` 的 `paths` frontmatter、
「200 行」是官方指引不是自創、壓縮後自動重讀、`@路徑` 匯入最多四層、
Hook 的 33 個事件與五種 handler 型別與離開碼 2、`Read(./.env)` 的 settings.json 語法、
案例三的 RLS 兩層與兩個錯誤訊息、競態解釋、Open-Meteo 示範、Git 四區與五個指令、
GitHub Pages 免費帳號只掛 public repo、Bun 案例四個數字。

---

## F. 涵蓋率的實話

三個 agent 都沒讀完全片。共同缺口兩塊，已補派 `gap-ch6`、`gap-ch7` 接手：
- 章節六預錄 `harness/11–22`（規則寫法與健檢）
- 章節七 `26a–26g`（報價系統演練，含資料庫與指令設計）
