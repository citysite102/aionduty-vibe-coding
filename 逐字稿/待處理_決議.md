# 待處理決議

逐條確認的結果。決議完成後再一次套用到 `src/`，不要邊確認邊改。

- **狀態**：`採用` ／ `採用（變體）` ／ `不改`
- 沒有出現在這份表上的單元，代表還沒確認過。

---

## 4-1 選修：終端機與基本指令（Slide 41-43）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | `10_M1_TerminalBasics` 左下說明區在預設分頁是空的 | 採用 | 補一句說明。寫「跑完會看到什麼」，不要寫成純操作交代（D-2） |
| 2 | `cd` 的說明把安裝頁講成已發生 | 採用 | 改成指向未來；順手掃這兩支選修頁的「那一頁／上一頁」 |
| 3 | `10a_M1_WarpTerminal` 兩個 `stepIndex={2}` | 採用 | 右邊 Warp 示範那塊（第 72 行）改成 `stepIndex={1}`，左邊三格維持 2、3、4 |
| 4 | 分節頁沒說「跳過會不會影響後面」 | 採用 | 補在 `SectionDivider` 的 `subtitle` 區，一句話不展開理由 |

## 4-2 選修：Claude Code 安裝與終端機專屬操作（Slide 44-45）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 權限模式名稱與循環順序沒有 C-1 註解 | 採用 | `11d2_M1_TerminalKeys` 與 `30_Cheat_Perms` 各補 C-1 三行，並各寫一行互相點名（改一邊要改兩邊） |
| 2 | 沒說登入要用哪一種帳號 | 採用 | 第三塊小字補半句「用你原本那個 Claude 訂閱帳號登入就好，不要填 API 金鑰」，理由不展開 |
| 3 | `/exit` 等寬字用 sky，違反 A-1 | 採用 | `/exit`、`claude` 改 orange；`cd`、`mission-timer`、`Ctrl + C` 改 `text-slate-300` |
| 4 | 章節四結束沒有送回主線的句子 | **不改** | 交給逐字稿的轉場口頭處理，畫面不動 |

## 5-1 運作框架（Harness）與它的六個零件（Slide 46-50）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | `20_M2_HarnessIntro` 借用 HTTP 狀態碼當編譯結果 | 採用（變體） | 兩個終點改成 `✗` / `✓`，不要借 404／200 的號碼 |
| 2 | 同頁兩組 `repeat: Infinity`（第 135、192 行） | 採用 | 改成跟「重新模擬」那顆按鈕綁在一起，觸發一次就停（`triggerCount` 已存在） |
| 3 | 全片 100 處 `text-[1Xpx]` | 採用 | **一次性整理**：`[11px]`、`[12px]` → `text-xs`；`[13px]`–`[15px]` 看上下文 → `text-sm`。改完跑 `npm run lint` 並肉眼看幾頁排版 |
| 4 | `19c` 的「沙箱」沒說由誰提供 | 採用（加碼） | 內文補「多半是公司或雲端平台幫你準備好的」；**另在檔頭註記 D-4：Harness 不要譯成「安全沙箱」**，因為這一頁兩個詞並排 |

## 5-2 Token 計費與付費模式（Slide 51-55）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | `11c_M1_PricingAndROI` 整頁價錢沒有 C-1 註解 | 採用 | 補 C-1 三行，點名 Anthropic pricing 與 Cursor pricing 兩個來源 |
| 2 | `/usage` 在 Slide 52 與 54 說法不一致 | 採用 | 以 Slide 52 為準。Slide 54 那張卡改成「打它看本期額度用掉多少、什麼時候重置」，拿掉「這次花了多少」 |
| 3 | 「快取讀取」沒有定義 | 採用 | 那行小字加括號：「快取讀取（同一份東西第二次讀進去會便宜很多）約輸入的十分之一」 |
| 4 | 三個浪費對三塊要建的東西，線只在註解裡 | 採用（變體） | **三格各標一行**，寫明它解的是哪一個浪費（不是只在開場補一句） |

## 5-3 MCP 與 Skills：Agent 的工具擴充（Slide 56-60）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | `/grill-me` 沒說來源 | 採用（來源已確認） | 來源：**Matt Pocock 的開源 skills 專案**。那一格補一行來源，寫法比照 Slide 57 的 `frontend-design`。套用前先核對倉庫名稱的正確拼法 |
| 2 | Skills「不會多出能力」與網頁測試打架 | 採用（變體） | **改寫 Slide 56 的絕對句**（不是在 Slide 60 補依賴說明）。把「不會多出能力」鬆成「不會自己多出能力，它可能要求你先裝好工具」 |
| 3 | `SKILL.md` 的欄位沒講過 | 採用 | 「自己寫一個」那一格補一句：檔案開頭要有 `name` 與 `description` 兩個欄位，名稱要跟資料夾一致 |
| 4 | Agent Plugins 那段的 C-1 註解 | 採用（**待重查**） | 先查 Claude Code 現在進沒進支援名單（那句是整段的結論），連內容一起更新，再寫 C-1 三行 |

## 5-4 監督程度與權限模式（Slide 61-62）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 標題「六個」vs 速查表四格 vs 內文「這四個」 | 採用（變體） | **補成六格**：`auto` 與 `dontAsk` 也做成卡片。標題不動，內文「就是下面這四個模式」要改成六個。版面會變滿，排版要重看 |
| 2 | 模擬器只演三格 | 採用（變體） | **把 `auto` 加進模擬器的循環**，跟文字一致。<br>⚠️ 實作註記：循環含 auto，但**起始狀態仍停在 `default`**，不要讓第一眼看到全放行。檔案第 1 行那段註解要跟著更新 |
| 3 | 與 `11d2` 同一批資料，兩頁都缺 C-1 | 採用 | 同 4-2 第 1 條，一起處理（兩支各補 C-1 三行並互相點名） |
| 4 | 旋鈕二沒說怎麼縮邊界 | 採用 | `31_Cheat_Dials` 最後那個 Callout 補一句：邊界靠限制它能用哪些工具、讓它只在一個資料夾裡工作。接上章節八的四道邊界 |

## 5-5 第一份 CLAUDE.md 實作（Slide 63-66）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 第 1 格沒預告草稿會太長 | **不改** | 順序本來就是先生再減，交給逐字稿口頭預告 |
| 2 | `/context` 模擬輸出沒標來源 | 採用 | 兩行各加灰色小標：「← 這個專案的」「← 全域，所有專案都吃得到」 |
| 3 | 替代路徑對整個單元不成立 | 採用 | 灰框搬到 Slide 63，改寫成涵蓋四格的版本（第 1 格改成「直接叫它幫你起一份空白的 `CLAUDE.md`」） |
| 4 | `Hook` 第一次出現沒有英文 | 採用（一勞永逸） | **Slide 49（`19c_Harness_Architecture`）那一格直接標「自動關卡（Hook）」**，讓這個字從第一次出現就帶著英文 |
| 5 | **（新增）** Slide 64 首句「它」出現四次無先行詞 | 採用 | `21b2_M2_HandsOnPrompt.tsx:43` 第一個「它」換成 `Claude`。位置代稱「上一頁第 1 格」這次不動 |

## 5-6 一份 CLAUDE.md 不夠用時：規則還能放哪幾層（Slide 67-72）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | `meta.script` 點名別段，`check:slides` 掃不到 | 採用 | `31_HandbookLength.tsx` 檔頭補一行，記下它指向 `harness/17_HealthOverview`。同樣做法套用到其他在 `script` 裡點名別段的頁 |
| 2 | Slide 69 四層與 Slide 65 的 `/context` 對不起來 | 採用 | 同 5-5 第 2 條一起處理，Slide 69 不用動 |
| 3 | 子目錄與 `CLAUDE.local.md` 沒有例子 | 採用 | 兩層都補。子目錄：「這一區的元件一律用 canvas 畫」；本機：「我這台機器用的是 Windows」或測試資料庫路徑 |
| 4 | Windows 替代做法方向與正文相反 | 採用 | 小字補：兩個檔案都會存在、規矩寫在 `AGENTS.md` 那一份、寫完用 `/context` 確認兩份都在清單裡。順手記進該頁既有的 C-1 註解 |

## 5-7 CLAUDE.md、Skill 與 MCP 的載入時機與存放位置（Slide 73-77）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 「誰叫 Skill 出來」三頁三種講法 | 採用 | Slide 74 的 Skill 那一格補半句：「要不要展開是 Claude 自己判斷的，你也可以直接點名」 |
| 2 | Rules 的範圍語法從沒出現過 | 採用 | Slide 73 的 Rules 那格路徑下補兩行示意（一行適用範圍＋一行規矩）。與 5-8 的 front matter 命名一起看 |
| 3 | 家目錄樹少了 `rules/` 與 `settings.json` | 採用（**待查**） | 先確認家目錄到底放不放得下這兩樣，再決定是補齊、還是改掉「兩邊結構長得一樣」那句 |
| 4 | Slide 76 三題 → 章節六四題的線只在口白 | 採用 | `21d4_M2_PartsMap.tsx` 檔頭補一行，記下與 `harness/04b_RouteIntro` 的對應（三題 vs 四題、多的那題是什麼、順序相反） |

## 5-8 運作框架在真實專案的應用（Slide 78-82）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | Slide 82 三個行號全錯（34/58/121 → 39/147/156） | 採用（變體） | **拿掉行號，改標章節編號 `A-1`、`A-4`、`A-3`**。比行號穩，也讓學員看得出那是一份有結構的檔案 |
| 2 | 第三條節錄照抄的是舊版 A-3 | 採用 | 縮成現在還存在的那一句：「特別注意 `.map()` 裡的條件式 class，一行程式可能生出七個閃爍點」 |
| 3 | front matter 用了三次沒給名字 | 採用 | **在 Slide 78 命名一次**：「開頭這段用兩條橫線夾起來的設定叫 front matter，Skill 的檔案開頭也長這樣」。連帶補起 5-3 ③ 與 5-7 ② |
| 4 | Slide 80「手冊寫什麼」與 Slide 66 判準打架 | 採用 | 欄位標題改成「手冊要定下來的是」或「要先講清楚的」，內容不動 |

## 6-1 規則失效的三種原因與規則分層（Slide 83-93）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | Slide 83 的 `meta.script` 以冒號開頭 | 採用 | 補主詞（例：「這一節從四個問題開始：⋯」），**並全片掃一次**有沒有別頁的 `script` 也以標點開頭。補完重跑 `check:rec`（現 22 秒，有空間） |
| 2 | 判準的人稱兩頁不一致 | 採用 | **統一成「你」**。Slide 84 改成「只看做出來的東西，你能不能回答有做到或沒做到」，`meta.script` 與畫面 `items` 一起改 |
| 3 | `@` 語法先用後定義 | 採用（變體） | **Slide 71 就給定義**（不只指路）：小字直接講明 `@` 是「把那個檔案的內容整份併進來」，Slide 85 再展開 |
| 4 | 三題分流 vs 四題分流沒對齊 | 採用 | `04b_RouteIntro` 補半句：「前面提過的那三題在這裡展開成四題，而且順序倒過來」。同步改 `meta.script` 並重跑 `check:rec` |

## 6-2 Hook：用程式強制執行規則（Slide 94-100）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | `matcher` 少了 `MultiEdit` | 採用 | Slide 95、97 的 matcher 改成 `Write\|Edit\|MultiEdit`，Slide 97 工具清單加一格 `MultiEdit ✓`。`65_HookMatcher` 檔頭註明它與 `.claude/settings.json` 綁在一起 |
| 2 | Slide 97 的補救說法跨了層 | 採用 | 畫面那句補結論半句：「路徑得在下一層的指令裡自己判斷」 |
| 3 | Hook 這一組的數字與 API 名稱沒有 C-1 | 採用 | `64_HookEvents`、`66_HookHandler`、`68_HookCodex` 各補 C-1 三行，點名 Claude Code hooks 文件與 Codex 對應文件 |
| 4 | Slide 99 驗收沒問掛在哪一層 | 採用 | 驗收清單加第三條「它寫進哪一個檔案了？」並補預期答案（專案的 `.claude/settings.json`），接回章節五的資料夾地圖 |

## 6-3 規則分層的取捨與第一次分流（Slide 101-103）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 口白說「留一行提醒」，成長軸沒示範 | 採用 | `_handbookVersions.ts` 把那條換成 `- 檔案刪除由 Hook 擋（見 .claude/settings.json）` 並標成新增。順便示範「提醒要指得出去哪裡找」 |
| 2 | Slide 101 是三十頁內第三套清單 | 採用 | 引言補半句「這是分流那四題的四個去處」，表格不加長 |
| 3 | Slide 102 訊號二與診斷頁重疊 | 採用 | 那一條補半句：「（這一種就是診斷裡『答得出來卻還是做錯』那一格）」 |
| 4 | Slide 77 與 Slide 110 沒互相指 | 採用 | `21d5_M2_FolderMap.tsx` 與 `56_HandbookV3.tsx` 各補一行註解互相點名，文案不動 |

## 6-4 CLAUDE.md 健檢流程與規則寫法（Slide 104-118）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | Slide 118 的 Prompt 只列五個技巧 | 採用 | 補一句括號：「探索空間那一條看的是時機，不在這一輪」。頁序不動，`meta.script` 一起改 |
| 2 | Slide 109 拿已搬走的規則當示範 | 採用 | 明說是同一件事：「這一條我們在分流那一段已經處理過了，健檢的第三步做的是同一件事」。順便說明兩套方法的關係 |
| 3 | Slide 111-116 六頁同構圖 | 採用 | 六頁各在標題列旁標「技巧 1／6」到「技巧 6／6」。不動 `_DontDo` 本身 |
| 4 | 「建議每三個月跑一次」只在口白 | 採用 | Slide 104 的流程軌旁補一行小字 |
| 5 | 「一律用繁體中文」不是矛盾，但兩頁沒互相指 | 採用 | 同 6-3 第 4 條，一起處理 |

## 6-5 CLAUDE.md 的跨工具轉移與從零建立（Slide 119-129）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | Slide 128 寫死 MCP 指令與網址 | 採用 | 補 C-1 三行，點名 `code.claude.com/docs/en/mcp` 安裝那一節與 Notion MCP 入門頁 |
| 2 | 轉移三題順序與分流四題相反 | 採用（改動較大） | **把三題順序調成跟分流一致**（會出事的先問）。Slide 122-126 四頁的畫面與 `meta.script` 都要改，改完重跑 `check:rec` |
| 3 | 轉移演練少了 Skill 那一層 | 採用 | Slide 126 對照表底下補一行：Skill 在這個例子還用不到，以及什麼時候會用到（產製步驟固定之後） |
| 4 | 「桌面版」前後詞義不一致 | 採用 | Slide 119 第三列補「也就是你前面一直在用的那個」，**並 grep 全片的「桌面版」**確認沒有別頁也指錯 |

## 7-1 Agent 分工：三個角色與四種模式（Slide 130-132）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 「執行者」與三種組織方式並列 | 採用 | 標題與引言定位成「把角色組起來的四種方式」，第二張改寫成「一次只派一個執行者」 |
| 2 | 三組 `repeat: Infinity` | **不改** | ⚠️ 與 5-1 第 2 條（`20_M2_HarnessIntro` 決議改成觸發一次就停）處置不同。同一條 A-3 兩頁兩種結果，套用時留意 |
| 3 | 審查者「只讀不寫」沒說怎麼給 | 採用（**待查**） | 先確認子代理設定檔支不支援限制工具。支援 → Slide 134 範例補那一行；不支援 → 改寫 Slide 131 並在 134 補驗收動作 |
| 4 | 路線圖第四塊「收成」 | 採用（變體） | **合進第三塊**，路線圖只畫三塊，收成寫進第三塊的 `note` |

## 7-2 品質防線：審查子代理實作（Slide 133-135）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | `25_M3_Quality.tsx:54` 同一句重複兩次 | 採用 | 刪掉重複那句，**並掃全片**找「同一句連續出現兩次」的地方 |
| 2 | 設定範例沒有權限欄位 | 採用（**待查**） | 同 7-1 第 3 條，一起處理 |
| 3 | 示範回覆的「第 42 行」 | 採用 | 換成不特定的寫法：「`index.html` 的倒數設定那一行」 |
| 4 | Slide 135 沒有「換成你的工作」 | 採用 | 最後那個 Callout 旁補一組對應：作者／讀者／觀察員，套在提案、教材、說明文件上。不展開做法 |

## 7-3 中型專案演練：需求拆解與指令設計（Slide 136-143）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | `quote-reviewer` vs `code-reviewer` 斷裂 | 採用 | Slide 140 或 141 補一句：「這個專案的審查者叫 `quote-reviewer`，建法跟你剛才那個 `code-reviewer` 一樣，只有標準不同」 |
| 2 | 沒說這一段該不該跟著做 | 採用 | Slide 136 補半句：「這一段看流程就好，不用開新專案；那五段指令之後要做比計時器大的東西時再回來抄」 |
| 3 | 「不用登入權限系統」與使用者設定打架 | 採用 | 改成寫出界線：「不用做登入，角色先用切換的模擬」 |
| 4 | 沒說什麼時候才接真資料庫 | 採用 | Slide 141 收尾補：「⋯也不接真的資料庫，那要等你確定欄位不會再改了」 |

## 8-1 工作交辦：目標、完成條件與邊界（Slide 144-146）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 「最多跑 8 輪」沒說寫在哪 | 採用 | 那一格補半句：「這三句直接寫進你交代的那段話裡，它會照著停」 |
| 2 | 「後面會專門講」指向四頁之後 | 採用 | `27_M4_LoopEng.tsx` 檔頭補註解，記下它指向 `28_M4_Safety` |
| 3 | 兩套「五」沒說關係 | 採用 | Slide 146 開場補半句區分：五個指令＝把專案切成五次交代；五個步驟＝每一次交代裡面要有什麼 |
| 4 | 路線圖第二塊的順序理由 | **不改** | 維持現狀，交給逐字稿口頭說明 |

## 8-2 介入時機、自動把關與安全邊界（Slide 147-150）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | Slide 148 開場那段是旁白語法不是投影片語法 | 採用（**改內文，標題不動**） | 詳見下方改寫草稿。問題是**後設導覽句**＋**自問自答**兩個疊在一起 |
| 2 | 四道邊界少了「錢」 | 採用（變體） | **補成五道**，加「在後台設用量上限」。⚠️ 標題「放手之前，先設好四道邊界」要一起改成五道，並同步 `App.tsx` 的 `LIVE_TITLES` |
| 3 | 紅字範例用 `Planet.tsx:24` | 採用 | 換成 `index.html`，行號留著，錯誤訊息形態不變 |
| 4 | `permissions` 語法與 Skill 名稱沒有查證註解 | 採用 | `28_M4_Safety` 點名 settings 文件那一節；`27b5_M4_NoCodeBridge` 記下 `webapp-testing` 的來源 |

### 8-2 ① 的改寫草稿（`27b5_M4_NoCodeBridge.tsx` 開場段）

**改前**
> 前面說「要有客觀的完成標準，迴圈才知道自己做完了沒」。問題是，你不會看程式碼，那個標準要從哪裡來？答案是：不用你來看。Lint、型別檢查與瀏覽器實測會替你跑，跑完給綠燈或紅字，AI 看得到同一份結果，紅字就自己回頭修。把關分兩種：一種是讀程式碼挑錯（下面兩個），一種是實際打開畫面點一次。你手上的計時器適用第二種，所以你要動手做的是那個。

**改後**
> 完成標準不必你自己驗。Lint、型別檢查與瀏覽器實測會替你跑，跑完是綠燈或紅字；AI 看得到同一份結果，紅字就自己回頭修。
> 把關分兩種：讀程式碼挑錯（下面兩個），或實際打開畫面點一次。你的計時器沒有前兩種跑得起來的結構，所以要動手做的是第三種。

**這次改的兩個毛病**（全片掃過，Slide 148 是唯一一處兩者疊在一起的）
- **後設導覽句**（D-2）：「前面說⋯」描述的是簡報自己的結構。
- **自問自答**：「問題是，⋯？答案是：⋯」是旁白的語法，投影片直接陳述即可。
- 附帶好處：把「前兩種為什麼不適用」從 JSX 註解搬到畫面上，原本只有維護的人看得到。

> **掃描結果（供日後參考）**：`23_M3_MultiAgent:55` 的「答案是」在 JSX 註解裡，畫面看不到；`29j_Case3_Race:44`「多數人的答案是『可以』」是刻意讓學員先答錯的設計；`12_M1_Example1:175`「前面說的『在本機跑』」指的是名詞不是頁面。三處都不用動。

## 8-3 自主循環實作：讓計時器自己跑完一輪（Slide 151-155）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 學員手上有兩份「完成標準」 | 採用 | Slide 151 補半句：「手冊裡那三題是每一次都要過的底線，這五題是這一輪額外的」。順便複習章節六的分層 |
| 2 | Slide 153 把學過的事講成新注意事項 | 採用 | 改成接住前面：「跟你剛才手寫那段一樣，輪數要自己寫進條件裡，它沒有內建上限」 |
| 3 | `localStorage` 只講好處不講限制 | 採用（變體） | **直接講明限制**（換一台裝置就不見、清瀏覽器資料也會清掉），不留懸念。<br>⚠️ 連帶：Slide 159 不再是意外而是印證，**那一頁的口白與 8-4 逐字稿的轉場句（「你等一下會發現還有一件事不對」）要改成印證的語氣** |
| 4 | Slide 154 用位置代稱指上一章 | 採用 | 改成內容指涉：「你在報價系統那個演練裡寫的那一頁需求說明」 |

## 8-4 部署上線：從本機到公開網址（Slide 156-160）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | `.gitignore` 沒說是誰寫的 | 採用（加碼） | 補半句講來源，**再加一句可貼的話**讓學員直接建。收尾 3-5 第 1 條那個缺口 |
| 2 | Slide 156 的 Prompt 寫死 `private` | 採用 | Prompt 改成「設成 private 或 public 先問我」。本身就是這一章教的 Human Gate |
| 3 | Slide 159 的延伸練習沒有入口 | 採用 | 補一句可貼的話：「幫我評估把這個計時器的紀錄改存到 Supabase 要做哪幾件事，先列步驟不要動檔案」 |
| 4 | 兩家平台的操作路徑與分頁名稱寫死 | 採用（加碼） | `27b8d_M4_Deploy` 與 `27b9_M4_ShipIt` 各補 C-1；Slide 159 四個分頁名稱旁補「名字用 Vercel 的叫法，其他家大同小異」（現在只寫在註解裡） |

## 8-5 案例一：器 VESSEL（Slide 161-164）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 十二步只有產出沒有動作 | 採用（局部） | 只把第 11 步寫具體：「一份問題清單，分成要修、可以不修、不是問題三類」。其餘不動 |
| 2 | 「三個案例共用同一份規格」只在副標 | 採用 | Slide 162 開場補半句：「這份規格三個案例共用，後面兩個案例交的是同一份，只有內容不同」 |
| 3 | 「不做什麼」第一次寫不出來 | 採用 | 第二塊的及格線補一句：「第一次寫不出三條的話，先做一版再回來補，案例一就是這樣長出來的」 |
| 4 | 三個案例的公開網址 | **確認後留著** | 網域是長期持有的，Slide 161／165／168 三頁的網址不動 |

## 8-6 案例二：東京環状 24 時（Slide 165-167）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 沒說十張照片哪裡來 | 採用 | 起點那行補半句：「照片可以是你自己拍的任何一個地方的一天，題目不必是東京」 |
| 2 | 外部來源那條沒升級成通則 | 採用 | Callout 補半句：「任何要拿出去的東西都一樣：外部來源在你控制不到的地方」，接上部署那一段 |
| 3 | Slide 166 與 167 第四列重複 | 採用 | Slide 167 第四列的 `why` 收短成「上一頁那條：補出來的動畫會互相打架」 |
| 4 | 三個案例的公開網址 | **確認後留著** | 同 8-5 第 4 條 |

## 8-7 案例三：開窯預約（Slide 168-170）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 資料庫的判準藏在品牌對照表裡 | 採用 | 表格上方或下方加一行：**「判斷基準：有帳號、有資料、有多人同時寫入，三個中一個就需要資料庫」**（用「判斷基準」不是「判準」） |
| 2 | 兩段錯誤訊息缺「接下來該做什麼」 | 採用 | 第一層補「去後台看那張表的存取設定」；第二層補「回去看那條政策放行的條件對不對」。接上 Slide 149 的三件事框架 |
| 3 | 三步沒指回循環 | 採用 | 三步那排補半句：「這就是前面那個循環，只是這次跑的人是你」 |
| 4 | 兩個測試帳號沒寫進實驗步驟 | 採用 | 第一步補半句：「開兩個瀏覽器、各自用一個測試帳號登入，同時按下去」 |

## 8-8 題目選擇與開工步驟（Slide 171-173）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 四段 Prompt 沒有複製按鈕 | 採用 | 四段各掛一顆 `CopyAction`（只出小圖示的那種，版面不會變滿） |
| 2 | 「一律用繁體中文回覆」與成長軸打架 | 採用 | 加括號：「（這條之後可以搬去家目錄那一份，每個專案就不用再寫）」，順便複習分層 |
| 3 | 「包成一份 `SKILL.md`」沒給做法 | 採用 | 直接給一段話：「把我們這一輪的做法整理成一份 `SKILL.md`，之後我說『用這個跑這個月的』你就照著做」 |
| 4 | 三頁的硬順序只有檔頭知道 | 採用 | **記在 `逐字稿/README.md`**，加一節「順序不能動的頁」，把這三頁與其他同類情況一起列 |

## 9-1 結語：未來的工作者（Slide 174）

| # | 項目 | 決議 | 要做的事 |
|---|---|---|---|
| 1 | 收尾沒指回手上的檔案 | 採用（變體） | **再畫一次資料夾樹**，跟 `26g_M3_Harvest` 同一種畫法，標出整門課的總產出 |
| 2 | 「前面那幾頁」是位置代稱 | 採用 | 改成內容指涉：「部署那一段有完整的步驟跟五個卡關的處理方式」 |
| 3 | 課後回饋連結的有效期 | 採用（變體） | **改指向課程平台**自己的回饋機制，不維護獨立表單。`FEEDBACK_FORM_URL` 與 QR code 一起換 |
| 4 | 「未來的工作者」內文沒接住 | 採用 | 微調主張那一句，收到「講得清楚的人，工具換幾輪都還在做決定」這個意思上。標題不動（對外文案） |

---

# 總結

**109 條全部確認完畢**（表上 110 列，多的一列是 5-5 ⑤，確認過程中新發現的）。

| 決議 | 條數 |
|---|---|
| 採用（含變體、加碼） | 104 |
| 不改 | 4 |
| 查證後維持現狀 | 2 |

其中 4 條要**先查證才動手**，見下方清單。

## 不改的四條（避免之後重新翻案）

| 出處 | 項目 | 理由 |
|---|---|---|
| 4-2 ④ | 章節四結束沒有送回主線的句子 | 交給逐字稿轉場口頭處理 |
| 5-5 ① | 第 1 格沒預告草稿會太長 | 順序本來就是先生再減 |
| 7-1 ② | Slide 132 三組 `repeat: Infinity` | ⚠️ 與 5-1 ② 處置不同，見該條註記 |
| 8-1 ④ | 路線圖第二塊的順序理由 | 交給逐字稿口頭說明 |

另有 2 條「查證後維持現狀」：8-5 ④ 與 8-6 ④，三個案例的公開網址確認為長期持有，畫面不動。

## 需要先查證才動手的（4 項）

1. **5-3 ①** `/grill-me` 的倉庫名稱（來源已知：Matt Pocock 的開源 skills 專案）
2. **5-3 ④** Claude Code 進沒進 Agent Plugins 支援名單 —— 那句是整段的結論
3. **5-7 ③** 家目錄放不放得下 `rules/` 與 `settings.json`
4. **7-1 ③／7-2 ②** 子代理設定檔支不支援限制工具 —— 決定審查者「只讀不寫」怎麼落地

## 跨單元的整批工作（不要逐頁改）

- **全片 100 處 `text-[1Xpx]`**（5-1 ③）：`[11px]`/`[12px]` → `text-xs`，`[13px]`–`[15px]` → `text-sm`
- **C-1 查證註解**：`11d2`、`30_Cheat_Perms`、`11c`、`21a`、`64_HookEvents`、`66_HookHandler`、`68_HookCodex`、`61_TransferIntegrate`、`28_M4_Safety`、`27b5`、`27b8d`、`27b9`
- **front matter 命名一次**（5-8 ③）：在 Slide 78 命名，連帶補起 5-3 ③ 與 5-7 ②
- **四次全片掃描**：`script` 以標點開頭（6-1 ①）、同一句連續出現兩次（7-2 ①）、「桌面版」詞義（6-5 ④）、無先行詞的「它」（已決定只改 Slide 64）

## 會動到標題的三條（要同步 `App.tsx` 的 `LIVE_TITLES`）

- **8-2 ②** 「放手之前，先設好四道邊界」→ 五道
- **5-4 ①** 速查表補成六格（標題不動，但內文「這四個模式」要改）

## 會動到 `meta.script` 的（要重跑 `npm run check:rec`）

6-1 ①②④、6-4 ①、6-5 ②（四頁）、8-3 ③ 連帶的 Slide 159

---

# 查證結果（2026-09-20）

## 1. `/grill-me` 的來源 ✅

- 倉庫：**`mattpocock/skills`**（GitHub，MIT 授權，38 個 skills）
- `grill-me` 是其中之一，作用是動工前反覆拷問你的計畫。
- **寫進 Slide 60**：比照 Slide 57 寫 `anthropics/claude-plugins-official` 的格式，寫 `mattpocock/skills`。
- ⚠️ 網路上流傳的安裝數與星數明顯失真，**不要寫進投影片**。

## 2. Agent Plugins 與 Claude Code ✅ 結論不變

- 官方站 `agent-plugins.org`：「Its initial Technical Steering Committee includes Core Maintainers from **Amazon, Cursor, Microsoft, OpenAI, and Vercel**.」
- Anthropic 與 Claude Code **都不在維護者名單，官方站也沒把它列為支援的 client**。
- 所以 Slide 56 現在寫的「Claude Code 目前不在支援名單裡，用的是自己的格式」**仍然正確，內容不用改**。
- 新資訊：**Google 後來加入了 TSC**（見 Google Developers Blog）。畫面上那五家是發布時的陣容，要不要補 Google 由講師決定。
- 二手新聞對這件事的說法互相矛盾（同一篇既說 Claude Code 保留自己的格式、又說 plugins 裝得進去），這正是 C-2 說的情況，以官方站為準。
- **只要補 C-1 三行**，來源寫 `agent-plugins.org`。

## 3. 家目錄的 `rules/` 與 `settings.json` ✅ 兩個都支援

- `~/.claude/rules/`：官方文件有「User-level rules」一節 ——「Personal rules in `~/.claude/rules/` apply to every project on your machine.」
- `~/.claude/settings.json`：使用者層設定檔，「You, in every project on this machine」。
- **所以 Slide 77 是漏列，「兩邊的結構長得一樣」那句話本身是對的**，補齊 `rules/` 與 `settings.json` 兩行即可。
- 附帶一個對得上的細節：「User-level rules are loaded before project rules, giving project rules higher priority.」——正好印證 Slide 69 的「同一件事講法不同時，下面的蓋過上面的」，兩頁可以互相指。

## 4. 子代理能不能限制工具 ✅ 能

- `.claude/agents/*.md` 的 front matter 支援：
  - **`tools`**：allowlist。「Inherits every tool available to subagents if omitted.」省略就是繼承全部。
  - **`disallowedTools`**：從繼承或指定的清單裡扣掉。
  - **`permissionMode`**：`default` / `acceptEdits` / `auto` / `dontAsk` / `bypassPermissions` / `plan`。
- **所以審查者可以真的做成唯讀**，不是只能靠文字約束。
- **寫進 Slide 134 的範例**：加一行 `tools: Read, Grep, Glob`，並在逐字稿點出「這一行才是防線，下面那句『不要自己動手改』只是提醒」。
- Slide 131 的「只給它讀的權限」**不用改**，它本來就是對的，只是缺了落地的那一行。

---

# 套用進度（2026-09-20）

## ✅ 第一步：四個查證 —— 完成

見上一節「查證結果」。四個全部有答案，其中兩個讓投影片的內容不用改（Agent Plugins 的結論、審查者的權限說法），兩個給出了具體要補的東西。

## ✅ 第二步：整批工作 —— 完成

### 全片字級整理
100 處 `text-[1Xpx]` 全部置換（19 個檔案）：`[11px]`×81、`[12px]`×5 → `text-xs`；`[13px]`×3、`[15px]`×11 → `text-sm`。四支檢查通過。

剩兩處不在原範圍、**沒有動**：`04b_LLM_Math_Limits:58` 的 `md:text-[38px]`、`12_M1_Example1:125` 的 `text-[64px]`。兩個都是單一展示級大字，不是 A-4 真正要防的「同頁三種差一兩 px」。要不要動待定。

### 三次全片掃描（結果比預期乾淨）

| 掃描 | 結果 |
|---|---|
| `script` 以標點開頭 | **只有 `21g_Div_Handbook` 一處**，與先前判斷一致 |
| 「桌面版」詞義 | **全片一致**。`10f_M1_DesktopFirst` 第 9、106 行已明講「是同一個 Claude Code，不是 Cowork」。只需 6-5 ④ 那半句 |
| 同句連續重複 | **只有 `25_M3_Quality:54` 一處** |

順帶查明：`21h2_M2_ProjectsInPractice` 在 `LIVE_SLIDES` 裡但落在已被預錄拆頁取代的 index 83，畫面上永遠不會出現，所以它那句「桌面版，綁一個本機資料夾」不構成詞義問題。

### C-1 註解 —— 12 支全部寫入

**已查證（7 支，掛 2026-09-20）**

| 檔案 | 查證結果 |
|---|---|
| `11d2_M1_TerminalKeys` | 六個模式、循環順序、Pro/Max 預設 auto **全部正確**，內容未改 |
| `30_Cheat_Perms` | 同上，另確認 `acceptEdits` 含 `mkdir`/`mv`/`cp`、`dontAsk` 不在循環中 |
| `21a_M2_MCPSkills` | Claude Code 仍不在 Agent Plugins 名單，**結論成立**；Google 後來加入 TSC |
| `64_HookEvents` | 官方 **33 個事件**，「三十幾種」成立；六個事件名稱全部存在 |
| `66_HookHandler` | 官方 **5 種 handler**，名稱逐項相符 |
| `68_HookCodex` | Claude Code 那一欄已核對；⚠️ Codex 那一欄未查 |
| `28_M4_Safety` | `Read(./.env)` 是文件原樣寫法，**正確** |

**未查證（5 支，掛「⚠️ 待查證」並列出要查什麼）**

`11c_M1_PricingAndROI`（部分查證：Pro $20、Max 5x $100 已確認；Max 20x、API 儲值下限、Cursor 價格未確認）、`61_TransferIntegrate`、`27b5_M4_NoCodeBridge`、`27b8d_M4_Deploy`、`27b9_M4_ShipIt`。

> 沒有掛假日期。C-1 的日期要是真的查過才有意義，未查的一律標成待查證並寫明下次要核對什麼。

### front matter 命名
`21e_M2_Extensions_Cases`（Slide 78）已命名，並點名 `SKILL.md` 與子代理的檔案開頭同結構 —— 連帶補起 5-3 ③ 與 5-7 ② 的缺口。

## ⏳ 第三步：逐單元改文案 —— 進行中

## ✅ 第三步：逐單元改文案 —— 完成

104 條全部套用，四支檢查通過。過程中有四件事跟決議時的預期不同，記在這裡：

### 1. 5-3 ① 是我的誤判，這一條不用改
`/grill-me` 的來源**本來就在畫面上**（`21a5_M2_PopularSkills.tsx:78` 的 `npx skills add mattpocock/skills --skill=grill-me`），而且正是查證到的那個倉庫。當初判斷「沒說來源」，是因為抽取腳本漏掉了那一行（它沒有中文、也沒有 `<code>` 標籤）。

### 2. 5-5 ④ 換了實作方式，效果相同
`19c_Harness_Architecture` 的每張卡本來就會印英文名（`{p.en}`），只是用 `text-slate-600` 幾乎看不見 —— 這才是「Hook 沒被認出來」的真正原因。所以改成：英文名由 `Hooks` 改單數 `Hook`，並把那一行提亮到 `text-slate-500`。等同決議要的「直接標」，又不會把同一個詞印兩次。

### 3. 6-5 ② 的連帶成本比預期大（已全部處理）
三題換順序不只動頁序，還牽動：`ProposalDraft` 的累積階段（手冊是照題目長出來的，最硬的那條現在先進檔案）、四頁的題號與 `stage`、`46_TransferMapping` 的對照表、`41_TransferCase` 的三格題目，以及 `registry.ts` 的頁序。另外兩處口白帶著舊順序的痕跡（「你的手冊就有第一批內容了」「這一條也會進手冊」），已改寫。

### 4. 9-1 ③ 做不完，留成待辦
課後回饋的 QR 是**離線算好的向量路徑**，換網址必須用 `qrcode` 重新產碼。我沒有課程平台的網址，猜一個會直接做出掃不到的 QR。已在 `FeedbackQR.tsx` 寫下待辦，並註明 `FEEDBACK_FORM_URL` 與 `PATH` 必須一起換。

### 順帶修掉的
- 我自己在兩處寫了中文破折號（`22_HealthRest`、`21f2_M2_ThisDeck`、`FeedbackQR`），違反 D-1。因為是透過 python 寫入，`.claude/settings.json` 的 hook 沒攔到。全片掃描後已清空（剩一處是英文副標的 em dash，不在 D-1 範圍）。
- `32_AgentsMd` 補了 `@` 的定義之後畫面中文超過 160 字，`check:rec` 擋下來，收短三次才過。`/context` 驗收那半句放不下，留在逐字稿口白裡。
- `30_Cheat_Perms` 補成六格時我一度把色相從 4 種推到 5 種（新增 indigo），已改成 `auto` 走 amber、`dontAsk` 走灰階，回到原本的色相數。

### 連帶更新的逐字稿
5-1、5-4、6-5、7-1、7-2、8-2、8-3、8-4 的畫面描述、口白與轉場句已同步。其中 6-5 的 Slide 123-125 是整段重排。

## 驗收

| 項目 | 結果 |
|---|---|
| `npm run lint` | 通過 |
| `npm run check:slides` | 通過（LIVE 132 頁 → 實際播放 174 頁，39 個單元） |
| `npm run check:rec` | 通過（1 個既有警告：`03_FailCantFollow` 有項目偏長） |
| `npm run check:words` | 通過 |
| A-1 色相 | 只有允許的那七個（`slate sky emerald amber orange indigo red/rose`） |
| A-4 自訂字級 | 只剩 2 處展示級大字（`text-[38px]`、`text-[64px]`），不在本輪範圍 |
| D-1 中文破折號 | 0 |
