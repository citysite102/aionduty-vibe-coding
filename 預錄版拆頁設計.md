# 預錄版拆頁設計

目的：把現行 96 頁的現場簡報，改製成符合預錄課判準的版本。
本文件決定的是**架構與規則**，不是逐頁內容。內容依規則拆解即可。

---

## 一、最重要的一個決定：固定畫布

### 現在的問題

現行簡報是流動版面（`max-w-6xl` 置中，Tailwind 的 rem 字級跟著瀏覽器 root font-size 走）。這代表**同一頁在不同視窗寬度下，文字的實際像素大小不一樣**。

錄製時如果瀏覽器視窗是 1920 CSS px 寬，`text-sm`（14px）輸出到 1080p 影片就是 14 個實體像素，換算 **7pt**。而畫面上的縮放控制只能到 150%，而且 `text-[11px]` 這類寫死 px 的字根本不跟著放大。

### 解法：1920x1080 固定舞台

預錄版的每一頁都畫在一個**固定 1920x1080 的容器**上，再用 CSS transform 縮放到瀏覽器視窗大小。

```jsx
// RecordedLayout 的核心
const scale = Math.min(vw / 1920, vh / 1080);

<div style={{ width: 1920, height: 1080, transform: `scale(${scale})`, transformOrigin: 'top left' }}>
  {/* 這裡面的 px 就是輸出影片的 px */}
</div>
```

這樣做的效果是**設計座標等於輸出座標**。你寫 40px，錄出來就是 40 個實體像素，等於 20pt，不需要再換算，也不受錄製時視窗大小影響。

**這是整個拆頁工程的前提。** 沒有固定畫布，字級規範無法驗證，拆頁也失去依據。

---

## 二、版面規格

### 2-1 安全區

以 1920x1080 為座標：

| 區域 | 範圍 | 用途 |
|---|---|---|
| 標題區 | 上方 0 到 200px | 頁面標題，內容不進入 |
| 內容區 | y 200 到 918，x 120 到 1440 | 唯一可以放內容的地方 |
| 講師人像遮蔽區 | 右下 480x270（x 1440 起，y 810 起） | 淨空 |
| 字幕安全區 | 下方 162px（y 918 起） | 淨空 |

實際可用的內容區是 **1320 x 718**，比想像中小很多。這正是逼出拆頁的機制。

### 2-2 字級（硬性下限）

| 用途 | 尺寸 | 換算 |
|---|---|---|
| 頁面標題 | 64px | 32pt |
| 內文 | 40px | 20pt |
| 註解、來源標註 | 40px | 20pt |
| 程式碼 | 36px 等寬 | 18pt |

**沒有第五種尺寸，也不存在小於 36px 的字。** 要做出層次請用顏色與粗細，不要用縮小。

程式碼是唯一的例外（等寬字在相同 pt 下視覺較大），但仍需人眼確認。

### 2-3 每頁內容上限

| 項目 | 上限 |
|---|---|
| 中文字數 | 120 字 |
| 條列項目 | 5 項，每項 20 字 |
| 主張 | **1 個** |

40px 中文在 1320px 寬的內容區約 33 字一行，120 字約 4 行。加上標題與留白，一頁就滿了。

### 2-4 一頁一個主張

判斷方法：**這一頁的標題能不能用一個肯定句寫完，而且沒有「與」「和」「以及」。**

有連接詞，就是兩頁。

---

## 三、怎麼拆：六種模式

現行每一頁先歸類，再照對應模式拆解。

### 模式 A　清單頁 → 一項一頁

原頁列出 N 個並列項目。

拆法：每項一頁，最後補一頁對照表（表格用逐列揭示，不要一次全出）。

**但先問一句**：這 N 項有沒有共同的分類軸？有的話按軸分組，會比一項一頁更好。M2 的六個零件就是這樣從六頁變三頁（依載入時機分組），學員記得住的是分類方式，不是清單。

### 模式 B　對照頁 → 三頁

原頁左右並列「錯的做法」與「對的做法」。

拆法：
1. 先出錯的那邊，停在問題上
2. 再出對的那邊
3. 第三頁講「差別在哪」

現場簡報可以一次看兩邊，影片不行，因為觀眾沒有掃視的時間。

### 模式 C　流程頁 → 一步一頁

原頁是 N 步驟的流程。

拆法：一步一頁，每頁重複顯示整條流程（當前步驟高亮，其餘灰階），讓中途加入的觀眾知道自己在哪。

### 模式 D　表格頁 → 每頁 3 列

原頁是多列表格。

拆法：每頁最多 3 列，欄位超過 3 欄要重新設計。表格是現場簡報的產物，影片裡觀眾讀不完。

### 模式 E　圖解頁 → 圖與說明分開

原頁是圖加上一段解釋。

拆法：圖獨立一頁（圖佔滿內容區），說明另一頁。需要標註時用逐項揭示，不要一次標滿。

### 模式 F　動手頁 → 三頁

原頁是一段操作指示。

拆法：
1. 要下的指令（一頁只有一個指令）
2. 你應該看到什麼（預期結果的畫面）
3. 沒看到的話怎麼辦（常見失敗）

第三頁是現行簡報最缺的，也是預錄課退課率最高的地方。

---

## 四、頁數估算

依 120 字上限，用現有內容的中文字數換算：

| 區塊 | 現行頁數 | 中文字數 | 預錄頁數 | 影片支數（10 頁一支） |
|---|---|---|---|---|
| 課前導讀 | 3 | 543 | 5 | 1 |
| 解構 Vibe Coding | 10 | 約 2,900 | 24 | 2 到 3 |
| M1 終端機實作 | 26 | 9,528 | 80 | 6 到 8 |
| M2 運作框架 | 42 | 約 14,500 | 121 | 9 到 12 |
| M4 循環工程 | 18 | 6,897 | 58 | 4 到 6 |
| 結語 | 1 | 203 | 2 | 1 |
| **合計** | **96** | **約 34,600** | **約 290** | **23 到 31** |

這個數字會再往上，因為模式 B、C、F 拆出來的頁面會新增文字（預期結果、常見失敗）。實際抓 **320 到 350 頁**比較安全。

---

## 五、專案結構

### 5-1 不要改現行檔案

現場版與預錄版的字級、留白、佈局規則直接衝突，混在同一套元件裡會互相干擾。而且 8/9 要用現場版。

```
src/
  components/
    SlideLayout.tsx          現場版，不動
    RecordedLayout.tsx       新增，固定畫布加安全區
  slides/                    現場版 96 頁，不動
  slides-recorded/
    00-intro/
      01-hook.tsx
      02-outcomes.tsx
    02-vibe/
    03-terminal/
    04-harness/
    05-loop/
    registry.ts              匯出有序陣列與每頁的 metadata
```

### 5-2 每一頁帶著自己的口白

預錄版的每一頁匯出一個 `meta`，把口白跟頁面放在一起：

```ts
export const meta = {
  id: 'harness-04-rules-routing',
  title: '這條規則該放哪',
  script: '拿到一條規則，照順序問四個問題...',  // 口白稿
  seconds: 35,                                  // 預估秒數
};
```

好處有三個：口白不會跟頁面脫節；`seconds` 加總就是影片長度，不用事後才發現一支影片變成 20 分鐘；`script` 可以直接產出字幕檔。

**口白長度上限建議 45 秒。** 超過表示這一頁還是塞太多。

### 5-3 切換方式

用網址參數切換，不要另開專案：

```
/aionduty-vibe-coding/            現場版
/aionduty-vibe-coding/?mode=rec   預錄版
```

`App.tsx` 依參數選擇 registry 與 Layout。兩版共用同一組 UI 元件與配色。

---

## 六、自動檢查

拆頁最大的風險是「拆到一半又開始塞」。靠肉眼看不出來，要用腳本擋。

建議寫一支 `scripts/check-recorded.mjs`，掃 `src/slides-recorded/`，違規就結束碼非零：

| 檢查項 | 規則 |
|---|---|
| 字級 | 出現小於 36px 的字級 → 失敗 |
| 字數 | 單頁中文超過 120 字 → 失敗 |
| 條列 | 超過 5 項，或單項超過 20 字 → 警告 |
| 標題 | 含「與」「和」「以及」→ 警告（可能是兩頁） |
| 口白 | `seconds` 超過 45 → 警告 |
| meta | 缺 `id`、`title`、`script`、`seconds` → 失敗 |
| 安全區 | 內容元素超出 1320x718 → 需人眼確認，腳本抓不到 |

安全區只能靠人眼或截圖比對。建議做一個「安全區疊層」開關，開啟後在畫面上疊半透明的遮蔽區與字幕區，檢查時打開就看得出有沒有壓到。

---

## 七、執行順序

### 第 0 階段：地基（拆頁前必須完成）

1. `RecordedLayout.tsx`：固定畫布、安全區、安全區疊層開關
2. `registry.ts` 與 `meta` 型別
3. `check-recorded.mjs` 檢查腳本
4. `?mode=rec` 切換

**接著拆一節試作，不要一次拆全部。** 建議拿 M2 的規則維護三頁（規則沒照做、規則放哪、手冊越肥），它們是最近寫的、結構最乾淨，拆完約 12 到 15 頁，剛好一支影片。

試作完成後回頭檢討規格，再往下走。這一步不能省，因為規格錯了會影響後面 300 頁。

### 第 1 到 5 階段：依序拆解

順序建議照**內容成熟度**而不是課程順序：

1. **M2 運作框架**（已審查最完整、剛整理過）
2. **M4 循環工程**（標題品質最好，改動最少）
3. **解構 Vibe Coding**（剛整理過）
4. **課前導讀與結語**（份量小）
5. **M1 終端機實作**（最大、最需要重寫口白，留到最後，那時你對規格最熟）

M1 留到最後還有一個理由：它的口白偏教科書，需要的不只是拆頁，是重寫。等前面四個區塊做完，你會清楚知道口白該怎麼寫。

---

## 八、先不要做的事

- **不要為了拆頁而動現行 96 頁。** 現場版是 8/9 的交付物，兩者分開演進
- **不要一次拆完再檢查。** 每完成一節就跑一次檢查腳本並實際錄一段試看
- **不要在拆頁階段同時改內容。** 先照現有內容拆，內容問題記在待辦清單，拆完再統一處理。同時做兩件事會讓你分不清楚問題出在拆法還是內容
