# 預錄版拆頁設計

目的：把現行 96 頁的現場簡報，改製成符合預錄課判準的版本。
本文件決定的是**架構與規則**，不是逐頁內容。內容依規則拆解即可。

---

## 一、共用同一份 Layout

決定：**現場版與預錄版共用 `SlideLayout`，不另做 `RecordedLayout`。**

維護兩套版面規則，長期會變成兩份都不準。代價是字級無法像固定畫布那樣精確換算成 pt，需要靠下面兩件事補足。

### 1-1 先修好縮放控制（前置作業）

`App.tsx` 的縮放控制是改 root font-size。Tailwind 的 `text-sm`、`text-base` 這類是 rem，會跟著放大；但寫死的 `text-[11px]`、`text-[13px]` 完全不動。

目前全片有 **253 處寫死的 px 字級，散在 100 頁裡的 45 頁**，其中 `text-[11px]` 佔 219 處。也就是說縮放拉到 150% 時，最小的那批字反而原地不動，跟其他字的落差還會變大。

替換對照：

| 寫死的字級 | 次數 | 換成 |
|---|---|---|
| `text-[11px]` | 219 | `text-xs` |
| `text-[12px]` | 13 | `text-xs` |
| `text-[15px]` | 12 | `text-base` |
| `text-[10px]` | 4 | `text-xs` |
| `text-[13px]` | 3 | `text-sm` |
| `text-[8px]` | 1 | `text-xs` |

**這件事排在 8/9 之後做。** 換完之後多數文字會微幅變大（11px 變 12px），45 個檔案同時變動需要逐頁確認有沒有溢出，時間不夠。現場投影的情境下目前字級是夠的。

### 1-2 錄製時用較小的視窗

錄製時把瀏覽器視窗開在 **1280 CSS px 寬**，輸出時放大到 1920x1080，等於整份簡報乘以 1.5 倍。再配合修好之後的縮放控制（上限 150%），`text-sm` 的實際大小會落在 15pt 上下。

這不會達到 24pt 的嚴格標準，但足以在桌機全螢幕與平板上閱讀。**手機觀看仍會吃力，這是共用同一份 Layout 的已知代價。**

### 1-3 因此字數上限是唯一的硬約束

沒有固定畫布，字級就無法用腳本精確驗證。所以拆頁的判斷完全落在**單頁 160 字**這條規則上，它同時決定了口白長度與資訊密度，必須嚴格執行。

---

## 二、版面規格

### 2-1 安全區

共用 Layout 的情況下，安全區用**比例**而不是絕對座標來想：

| 區域 | 比例 | 做法 |
|---|---|---|
| 講師人像遮蔽區 | 右下 25% x 25% | 該區不放關鍵資訊。雙欄佈局的右欄底部要留白 |
| 字幕安全區 | 下方 15% | `SlideLayout` 的 `pb-20`（80px）需加大到約 15% |
| 操作列 | 右下角固定 | 錄製時需隱藏，見 5-3 |

目前 96 頁中多數採雙欄佈局、右下角有實質內容，這是拆頁時要一併調整的部分。單欄佈局最省事，也順帶壓低單頁資訊量。

### 2-2 字級

共用 Layout，所以沿用現行的 Tailwind 階層，規則改成：

- **不要使用寫死的 `text-[NNpx]`**，一律用 `text-xs` 以上的 rem class，縮放控制才有效
- **`text-xs` 是下限**，不要再往下
- 標題用 `text-3xl` 以上

### 2-3 每頁內容上限

| 項目 | 上限 |
|---|---|
| 中文字數 | 160 字 |
| 條列項目 | 5 項，每項 20 字 |
| 主張 | **1 個** |

沒有固定畫布可以驗證字級，所以 160 字是唯一能自動檢查的硬約束，必須嚴格執行。它同時決定了口白長度：160 字唸出來約 40 秒，剛好落在單頁口白 45 秒上限之內，兩個規則是一致的。

### 2-4 一頁一個主張

判斷方法：**這一頁的標題能不能用一個肯定句寫完，而且沒有「與」「和」「以及」。**

有連接詞，就是兩頁。

### 2-5 標題問問題，內文回答

**同一個系列的頁面共用一個問句當標題，各自的答案放在內文第一行。**

```
標題　　為什麼規則沒有生效？        ← 整個系列共用
內文　　原因 2 / 3                ← 小字編號
　　　　因為它排在三百行的後面，份量不夠。  ← 這一頁的答案，內容區最大的字
```

這樣做的理由是，標題如果要同時「講清楚這頁教什麼」「表明自己是系列的一員」「而且精確」，三個目標會互相打架，最後生出「有兩種情況，它根本沒看到」這種涵蓋不準的句子。拆開之後每個位置只做一件事，答案行也不再受標題長度限制，可以寫得更精確。

**但必須配一組編號。** 四頁標題相同時，下拉選單裡分不出來，中途進來的觀眾也不知道自己在哪一段。編號要跟答案放在一起（`原因 2 / 3` 就在答案上方），單獨在標題裡寫「第二種情況」不算，那是位置指涉，觀眾不知道是哪三種之中的第二種。

流程型的系列再加一條流程軌，見 `_RouteRail.tsx`。

### 2-6 一頁只有一個白色粗體

畫面上的層級要一眼分得出來，做法是限制最強的那一級只出現一次：

| 角色 | 樣式 | 一頁幾個 |
|---|---|---|
| 這一頁的答案或主張 | `text-3xl`／`text-4xl` 白色粗體 | **1 個** |
| 引導用的問句、標籤 | `text-2xl` `text-slate-300` 不加粗 | 不限 |
| 關鍵字、去處 | `text-sky-400` 粗體 | 1 到 2 個 |
| 說明 | `text-xl` `text-slate-400` | 不限 |

如果一頁出現兩個白色粗體，觀眾會分不出哪個才是重點。常見的成因是「主張」跟「去處」被寫成兩句意思重複的話，這時候應該合併，不是靠字級去區分。

---

## 三、怎麼拆：六種模式

現行每一頁先歸類，再照對應模式拆解。

### 模式 A　清單頁 → 一項一頁

原頁列出 N 個並列項目。

拆法：每項一頁，最後補一頁對照表（表格用逐列揭示，不要一次全出）。

**但先問一句**：這 N 項有沒有共同的分類軸？有的話按軸分組，會比一項一頁更好。M2 的六個零件就是這樣從六頁變三頁（依載入時機分組），學員記得住的是分類方式，不是清單。

### 模式 B　對照頁 → 三頁

原頁左右並列「錯的做法」與「對的做法」。

拆法：
1. 先出錯的那邊，停在問題上
2. 再出對的那邊
3. 第三頁講「差別在哪」

現場簡報可以一次看兩邊，影片不行，因為觀眾沒有掃視的時間。

### 模式 C　流程頁 → 一步一頁

原頁是 N 步驟的流程。

拆法：一步一頁，每頁重複顯示整條流程（當前步驟高亮，其餘灰階），讓中途加入的觀眾知道自己在哪。

### 模式 D　表格頁 → 每頁 4 列

原頁是多列表格。

拆法：每頁最多 4 列，欄位超過 3 欄要重新設計。表格是現場簡報的產物，影片裡觀眾讀不完。

### 模式 E　圖解頁 → 圖與說明分開

原頁是圖加上一段解釋。

拆法：圖獨立一頁（圖佔滿內容區），說明另一頁。需要標註時用逐項揭示，不要一次標滿。

### 模式 F　動手頁 → 三頁

原頁是一段操作指示。

拆法：
1. 要下的指令（一頁只有一個指令）
2. 你應該看到什麼（預期結果的畫面）
3. 沒看到的話怎麼辦（常見失敗）

第三頁是現行簡報最缺的，也是預錄課退課率最高的地方。

---

## 四、頁數估算

依 160 字上限，用現有內容的中文字數換算：

| 區塊 | 現行頁數 | 中文字數 | 預錄頁數 | 影片支數（10 頁一支） |
|---|---|---|---|---|
| 課前導讀 | 3 | 543 | 5 | 1 |
| 解構 Vibe Coding | 10 | 2,949 | 23 | 2 |
| M1 終端機實作 | 26 | 9,528 | 72 | 6 到 7 |
| M2 運作框架 | 38 | 14,472 | 112 | 9 到 11 |
| M4 循環工程 | 18 | 6,897 | 51 | 4 到 5 |
| 結語 | 1 | 203 | 2 | 1 |
| **合計** | **96** | **34,592** | **265** | **23 到 27** |

這個數字會再往上，因為模式 B、C、F 拆出來的頁面會新增文字（預期結果、常見失敗）。實際抓 **290 到 300 頁**比較安全。

**160 字這個上限最重要的效果不在總頁數，而在拆法。** 全片有 71 頁的 `stepIndex` 節拍數已經大於等於建議頁數，可以直接照節拍切開，不必重新設計結構。120 字的時候只有 41 頁符合。逐頁分析見 `逐頁拆頁評估.md`。

---

## 五、專案結構

### 5-1 頁面分開，Layout 共用

`SlideLayout` 兩版共用，但頁面檔案分開。理由是 8/9 要用現行版本，拆頁過程中它必須保持可用。

```
src/
  components/
    SlideLayout.tsx          兩版共用
  slides/                    現行 96 頁，8/9 用這份
  slides-recorded/
    00-intro/
      01-hook.tsx
      02-outcomes.tsx
    02-vibe/
    03-terminal/
    04-harness/
    05-loop/
    registry.ts              匯出有序陣列與每頁的 metadata
```

拆頁完成、實體課跑完之後，再決定要不要讓預錄版取代現行版，或兩份長期並存。

### 5-2 每一頁帶著自己的口白

預錄版的每一頁匯出一個 `meta`，把口白跟頁面放在一起：

```ts
export const meta = {
  id: 'harness-04-rules-routing',
  title: '這條規則該放哪',
  script: '拿到一條規則，照順序問四個問題...',  // 口白稿
  seconds: 35,                                  // 預估秒數
};
```

好處有三個：口白不會跟頁面脫節；`seconds` 加總就是影片長度，不用事後才發現一支影片變成 20 分鐘；`script` 可以直接產出字幕檔。

**口白長度上限建議 45 秒。** 超過表示這一頁還是塞太多。

### 5-3 切換方式

用網址參數切換，不要另開專案：

```
/aionduty-vibe-coding/            現行版
/aionduty-vibe-coding/?mode=rec   預錄版
/aionduty-vibe-coding/?mode=rec&clean=1   預錄版，隱藏操作列
```

`App.tsx` 只依參數選擇 registry，Layout 是同一個。`clean=1` 隱藏右下角的頁碼、翻頁鈕與縮放控制，錄製時用，避免入鏡。

---

## 六、自動檢查

拆頁最大的風險是「拆到一半又開始塞」。靠肉眼看不出來，要用腳本擋。

建議寫一支 `scripts/check-recorded.mjs`，掃 `src/slides-recorded/`，違規就結束碼非零：

| 檢查項 | 規則 |
|---|---|
| 字級 | 出現寫死的 `text-[NNpx]` → 失敗 |
| 字數 | 單頁中文超過 160 字 → 失敗 |
| 條列 | 超過 5 項，或單項超過 20 字 → 警告 |
| 標題 | 含「與」「和」「以及」→ 警告（可能是兩頁） |
| 口白 | `seconds` 超過 45 → 警告 |
| meta | 缺 `id`、`title`、`script`、`seconds` → 失敗 |
| 安全區 | 右下 25% 有沒有實質內容 → 需人眼確認，腳本抓不到 |

安全區只能靠人眼或截圖比對。建議做一個「安全區疊層」開關，開啟後在畫面上疊半透明的遮蔽區與字幕區，檢查時打開就看得出有沒有壓到。

---

## 七、執行順序

### 第 0 階段：地基（拆頁前必須完成）

1. `registry.ts` 與 `meta` 型別
2. `check-recorded.mjs` 檢查腳本
3. `?mode=rec` 切換與 `clean=1` 隱藏操作列
4. 安全區疊層開關（在畫面上疊出人像遮蔽區與字幕區，檢查時打開）

另外有一件前置作業可以獨立進行：**把全片 253 處寫死的 px 字級換成 rem class**，讓縮放控制對整份簡報生效。這件事對現行版與預錄版都有好處，但要在 8/9 之後做，因為換完需要逐頁確認有沒有溢出。

**接著拆一節試作，不要一次拆全部。** 建議拿 M2 的規則維護三頁（規則沒照做、規則放哪、手冊越肥），它們是最近寫的、結構最乾淨，拆完約 12 到 15 頁，剛好一支影片。

試作完成後回頭檢討規格，再往下走。這一步不能省，因為規格錯了會影響後面 300 頁。

### 第 1 到 5 階段：依序拆解

順序建議照**內容成熟度**而不是課程順序：

1. **M2 運作框架**（已審查最完整、剛整理過）
2. **M4 循環工程**（標題品質最好，改動最少）
3. **解構 Vibe Coding**（剛整理過）
4. **課前導讀與結語**（份量小）
5. **M1 終端機實作**（最大、最需要重寫口白，留到最後，那時你對規格最熟）

M1 留到最後還有一個理由：它的口白偏教科書，需要的不只是拆頁，是重寫。等前面四個區塊做完，你會清楚知道口白該怎麼寫。

---

## 八、先不要做的事

- **不要為了拆頁而動現行 96 頁。** 現場版是 8/9 的交付物，兩者分開演進
- **不要一次拆完再檢查。** 每完成一節就跑一次檢查腳本並實際錄一段試看
- **不要在拆頁階段同時改內容。** 先照現有內容拆，內容問題記在待辦清單，拆完再統一處理。同時做兩件事會讓你分不清楚問題出在拆法還是內容
